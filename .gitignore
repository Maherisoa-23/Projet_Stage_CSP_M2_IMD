# ===== Python =====
**/__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

final_h3_h9.db.backup-before-dihedral
rapport_benchmark_csp.md
script choco vs ace/
img rapport de stage/
presentation/

# Donnees brutes a la racine (les .graph des h6-h9 sont dans
# experiments/v1/plane/benzdb/, gitignored separement)
/data/

# ===== Environnement Python =====
venv/
.venv/

# ===== Outils IDE / agents =====
.claude/
.vscode/
.idea/
*.swp
*.swo

# ===== OS =====
.DS_Store
Thumbs.db

# ===== Bases SQLite (regenerables via viewer/build_db.py) =====
*.db
*.db-shm
*.db-wal

# ===== Logs =====
*.log

*.aux

# ===== Dossiers de travail jetables =====
# Scripts temporaires et fichiers intermediaires (rebuild DB, count cluster,
# sync checks, etc.). Tout ce qui est jetable apres run.
tmp/
# Archives diverses (anciens fichiers, code deplace lors des refactors).
divers/
# Docs recuperes du commit a67f065^ (rapport de stage + notes chimie + tex).
# Volontairement hors-git : archive locale de reference.
doc from commit/

# ===== Inputs lourds des experimentations (regenerables, ~10 MB+) =====
experiments/v1/plane/
experiments/v1/experiments_bundle.zip

# ===== Pipelines experimentaux abandonnes (v1/v2/v3, remplaces par =====
# ===== viewer/ + csp_solver/final/) — ne pas re-tracker. =====
experiments/v1/
experiments/v2/
experiments/v3/

# ===== Scripts cluster one-shot (SSH/SLURM, lances manuellement, pas =====
# ===== importes par le pipeline) — ne pas re-tracker. =====
cluster/ops/

# ===== Scripts/figures one-shot lies a la redaction (memoire, bench) =====
scripts/figures/
scripts/migrate_to_dihedral_verdicts.py
scripts/recompute_dihedrals.py
*.png

# ===== Sorties d'experimentations (regenerables via run_one_job) =====
experiments/**/output/
experiments/**/_runs/
experiments/**/_results/
experiments/**/_run_v2/
experiments/**/_ev2_run/
experiments/**/_ev3_run/
experiments/**/_ev3_val/

# ===== Viewer =====
# Le code dans viewer/ est tracke (731 fichiers indexes avant que cette regle
# soit ajoutee). Cette regle empeche les NOUVEAUX dossiers (viewer/output/,
# viewer/missing_export/, viewer/__pycache__/, etc.) d'etre tracks.
viewer/

# ===== doc/ : artefacts d'experimentations (PDFs, captures, .tex generes) =====
# Tout ce qu'on a genere durant les experimentations CSP/motifs est ignore.
# Seul experimentation.md (synthese) est versionne dans doc/.
# Les fichiers de doc/ pre-existants (memoire.tex, etc.) restent tracks
# normalement -- .gitignore n'affecte pas les fichiers deja indexes.
doc/captures/
doc/motifs_bord_h8.tex
doc/motifs_bord_h8.pdf

# Docs personnels d'auto-formation pour la soutenance (NE PAS PUSH)
doc/apprentissage_*.md

# ===== Rapports de stage / memoire (redaction, pas du code) =====
doc/memoire.tex
doc/memoire.pdf
doc/explication_lex_leader.tex
doc/explication_lex_leader.pdf
doc/modele_csp_kekule_clar.tex
divers/old_resume_standalone.tex
divers/old_resume_standalone.pdf
experiments/v2/rapport_exp_v2.tex
experiments/v3/rapport_exp_v3.tex
*.pdf


# ===== PyCSP3 generated XML (race condition entre test CLI et CSP) =====
main.xml
model.xml
