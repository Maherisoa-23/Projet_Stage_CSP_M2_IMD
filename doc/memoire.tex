% =====================================================================
%  Mémoire de stage M2 IMD — Toky Nirina RAMAHERISOA — 2025/2026
% =====================================================================
\documentclass[11pt,a4paper]{article}

% ---------- Encodage / langue ----------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[french]{babel}

% ---------- Mise en page ----------
\usepackage[margin=2.4cm]{geometry}
\usepackage{setspace}
% Alinea en debut de paragraphe (style these francaise) -- pas de parskip
\setlength{\parindent}{1.5em}
\setlength{\parskip}{0.3em}

% ---------- Maths / tableaux / figures ----------
\usepackage{amsmath,amssymb}
\usepackage{amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{enumitem}
% Forcer puces rondes pour itemize (au lieu des em-dashes du francais babel)
\setlist[itemize,1]{label=$\bullet$}
\setlist[itemize,2]{label=$\circ$}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc,arrows.meta}
% Environnement algorithm (float dedie + liste des algorithmes)
\usepackage{algorithm}
\floatname{algorithm}{Algorithme}
\renewcommand{\listalgorithmname}{Liste des algorithmes}

% ---------- Liens ----------
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Memoire M2 IMD - Generation PPC de non-benzenoides 5/6/7},
  pdfauthor={Toky Nirina Ramaherisoa}
}

% =====================================================================
%  Macros
% =====================================================================
\definecolor{descrgray}{gray}{0.35}
\newcommand{\descr}[1]{%
  {\small\itshape\color{descrgray}\textbf{Contenu prévu :} #1\par}\vspace{0.4em}}
\newcommand{\todo}[1]{{\bfseries\color{red}[À COMPLÉTER : #1]}}
\newcommand{\tbd}{{\bfseries\color{red}--?--}}  % cellule de tableau à remplir

\newcommand{\GD}{\ensuremath{G_D}}                 % graphe dual
\newcommand{\Tn}{\ensuremath{\mathcal{T}}}         % table de voisinage
\newcommand{\xtb}{\textsc{xTB}}
\newcommand{\ace}{\textsc{ace}}
\newcommand{\Aut}{\operatorname{Aut}}

\newtheorem{definition}{Définition}
\newtheorem{remarque}{Remarque}

\begin{document}

% =====================================================================
%  PAGE DE GARDE
% =====================================================================
\begin{titlepage}
\centering
% \includegraphics[height=1.7cm]{amu.png}\hfill
% \includegraphics[height=1.7cm]{lis.png}\hfill
% \includegraphics[height=1.7cm]{ism2.png}
{\large\bfseries Aix-Marseille Université\quad$\cdot$\quad LIS\quad$\cdot$\quad iSm2}

\vspace{1.2cm}
{\large Mémoire de stage de Master 2}\\[0.2em]
{\large Informatique et Mathématiques Discrètes (IMD)}\\[0.2em]
{\large Année universitaire 2025--2026}

\vspace{1.6cm}
\rule{\linewidth}{0.5pt}\\[0.6em]
{\LARGE\bfseries Génération automatique et exhaustive de structures\\[0.25em]
moléculaires bi-dimensionnelles par programmation\\[0.25em]
par contraintes\par}
\vspace{0.5em}
\rule{\linewidth}{0.5pt}

\vspace{1.6cm}
{\large\bfseries Toky Nirina RAMAHERISOA}

\vspace{1.4cm}
\begin{tabular}{rl}
\textbf{Encadrants} & Nicolas \textsc{Prcovic} et Cyril \textsc{Terrioux}\\
                    & \emph{LIS, équipe COALA, Aix-Marseille Université}\\[0.6em]
\textbf{Partenaires} & Yannick \textsc{Carissan} et Denis \textsc{Hagebaum-Reignier}\\
                     & \emph{Institut des Sciences Moléculaires de Marseille (iSm2)}\\
\end{tabular}

\end{titlepage}

\tableofcontents
\newpage
\listoffigures
\listoftables
\listofalgorithms
\newpage

% =====================================================================
\section{Introduction}
% =====================================================================

\subsection{Contexte}

Les \emph{hydrocarbures aromatiques polycycliques} (HAP) sont des molécules
constituées uniquement de carbone et d'hydrogène, dont les atomes de carbone
forment des cycles fusionnés. Ils sont étudiés pour leurs propriétés
électroniques et opto-électroniques. Parmi les HAP, les \emph{benzénoïdes}
forment la sous-famille composée exclusivement d'hexagones fusionnés par une
arête. Cette famille a fait l'objet de nombreux travaux, et notamment de la
thèse de A.~Varet~\cite{varet2022}, qui a montré que la \emph{programmation
par contraintes} (PPC) est un outil particulièrement adapté à la génération
exhaustive de benzénoïdes répondant à des propriétés données.

L'étude des HAP comportant des cycles à \emph{cinq} ou \emph{sept} atomes,
dits \emph{non-benzénoïdes}, est plus récente. L'azulène, formé d'un
pentagone et d'un heptagone accolés (figure~\ref{fig:azulene}), en est
l'exemple emblématique. L'introduction de tels cycles modifie en profondeur
la chimie de la molécule : elle brise la régularité du pavage hexagonal,
induit une courbure locale, et favorise l'apparition de sites radicalaires.
Ces structures intéressent donc les chimistes, mais leur génération
systématique reste largement inexplorée.

\begin{figure}[h]
\centering
% Style global pour les atomes C (utilisé dans les deux schémas)
\tikzset{Catom/.style={inner sep=1pt, fill=white, font=\scriptsize}}

\begin{tikzpicture}[scale=1.105, baseline=(current bounding box.center)]
  % --- Pentagone regulier a gauche ---
  \pgfmathsetmacro{\rP}{1.4/(2*sin(36))}
  \pgfmathsetmacro{\hP}{\rP*cos(36)}
  \coordinate (CP) at (-\hP, 0);
  \coordinate (P0) at ($(CP)+({\rP*cos(36)},{\rP*sin(36)})$);
  \coordinate (P1) at ($(CP)+({\rP*cos(108)},{\rP*sin(108)})$);
  \coordinate (P2) at ($(CP)+({\rP*cos(180)},{\rP*sin(180)})$);
  \coordinate (P3) at ($(CP)+({\rP*cos(252)},{\rP*sin(252)})$);
  \coordinate (P4) at ($(CP)+({\rP*cos(324)},{\rP*sin(324)})$);
  \draw (P0)--(P1)--(P2)--(P3)--(P4)--cycle;
  \node at (CP) {\small $5$};

  % --- Heptagone regulier a droite ---
  \pgfmathsetmacro{\rH}{1.4/(2*sin(180/7))}
  \pgfmathsetmacro{\hH}{\rH*cos(180/7)}
  \pgfmathsetmacro{\angA}{180-(180/7)}
  \coordinate (CH) at (\hH, 0);
  \coordinate (H0) at ($(CH)+({\rH*cos(\angA)},{\rH*sin(\angA)})$);
  \coordinate (H1) at ($(CH)+({\rH*cos(\angA-360/7)},{\rH*sin(\angA-360/7)})$);
  \coordinate (H2) at ($(CH)+({\rH*cos(\angA-2*360/7)},{\rH*sin(\angA-2*360/7)})$);
  \coordinate (H3) at ($(CH)+({\rH*cos(\angA-3*360/7)},{\rH*sin(\angA-3*360/7)})$);
  \coordinate (H4) at ($(CH)+({\rH*cos(\angA-4*360/7)},{\rH*sin(\angA-4*360/7)})$);
  \coordinate (H5) at ($(CH)+({\rH*cos(\angA-5*360/7)},{\rH*sin(\angA-5*360/7)})$);
  \coordinate (H6) at ($(CH)+({\rH*cos(\angA-6*360/7)},{\rH*sin(\angA-6*360/7)})$);
  \draw (H0)--(H1)--(H2)--(H3)--(H4)--(H5)--(H6);
  \node at (CH) {\small $7$};

  % --- Hydrogenes (places radialement) + traits C-H ---
  \pgfmathsetmacro{\rPh}{\rP+0.45}
  \pgfmathsetmacro{\rPt}{\rP+0.20}     % position de depart du trait depuis le sommet
  \pgfmathsetmacro{\rHh}{\rH+0.45}
  \pgfmathsetmacro{\rHt}{\rH+0.20}
  \tikzset{Hatom/.style={font=\scriptsize, inner sep=1pt, fill=white}}
  \tikzset{bondCH/.style={line width=0.4pt}}
  % Pentagone : H sur P1, P2, P3 (les sommets non partages avec l'heptagone)
  \foreach \ang in {108, 180, 252} {
    \draw[bondCH] ($(CP)+({\rPt*cos(\ang)},{\rPt*sin(\ang)})$)
              -- ($(CP)+({\rPh*cos(\ang)-0.10*cos(\ang)},{\rPh*sin(\ang)-0.10*sin(\ang)})$);
    \node[Hatom] at ($(CP)+({\rPh*cos(\ang)},{\rPh*sin(\ang)})$) {H};
  }
  % Heptagone : H sur H1, H2, H3, H4, H5
  \foreach \k in {1,2,3,4,5} {
    \pgfmathsetmacro{\ang}{\angA-\k*360/7}
    \draw[bondCH] ($(CH)+({\rHt*cos(\ang)},{\rHt*sin(\ang)})$)
              -- ($(CH)+({\rHh*cos(\ang)-0.10*cos(\ang)},{\rHh*sin(\ang)-0.10*sin(\ang)})$);
    \node[Hatom] at ($(CH)+({\rHh*cos(\ang)},{\rHh*sin(\ang)})$) {H};
  }

  % --- Annotations C sur les carbones (par-dessus, fond blanc pour masquer le trait) ---
  \node[Catom] at (P0) {C};
  \node[Catom] at (P1) {C};
  \node[Catom] at (P2) {C};
  \node[Catom] at (P3) {C};
  \node[Catom] at (P4) {C};
  \node[Catom] at (H1) {C};
  \node[Catom] at (H2) {C};
  \node[Catom] at (H3) {C};
  \node[Catom] at (H4) {C};
  \node[Catom] at (H5) {C};
\end{tikzpicture}
\hspace{1.2cm}
\begin{tikzpicture}[scale=0.55, baseline=(current bounding box.center)]
  \def\rhex{1.2}
  \pgfmathsetmacro{\apo}{\rhex*cos(30)}
  \pgfmathsetmacro{\dyhex}{1.5*\rhex}
  \pgfmathsetmacro{\rOut}{\rhex+0.45}
  \pgfmathsetmacro{\rIn}{\rhex+0.20}
  \tikzset{Hatomb/.style={font=\scriptsize, inner sep=1pt, fill=white}}
  \tikzset{bondCHb/.style={line width=0.4pt}}
  % v0 -- carbones libres (sans voisin) : k=2,3,4
  \begin{scope}[shift={(0,0)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \foreach \k in {2,3,4} {
      \pgfmathsetmacro{\ang}{60*\k+30}
      \draw[bondCHb] ({cos(\ang)*\rIn},{sin(\ang)*\rIn}) -- ({cos(\ang)*\rOut},{sin(\ang)*\rOut});
      \node[Hatomb] at ({cos(\ang)*(\rOut+0.15)},{sin(\ang)*(\rOut+0.15)}) {H};
    }
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
  % v1 -- hexagone entierement interne, aucun hydrogene
  \begin{scope}[shift={(2*\apo,0)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
  % v2 -- libres : k=0,1
  \begin{scope}[shift={(4*\apo,0)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \foreach \k in {0,1} {
      \pgfmathsetmacro{\ang}{60*\k+30}
      \draw[bondCHb] ({cos(\ang)*\rIn},{sin(\ang)*\rIn}) -- ({cos(\ang)*\rOut},{sin(\ang)*\rOut});
      \node[Hatomb] at ({cos(\ang)*(\rOut+0.15)},{sin(\ang)*(\rOut+0.15)}) {H};
    }
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
  % v3 -- libres : k=0,1,2
  \begin{scope}[shift={(\apo, \dyhex)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \foreach \k in {0,1,2} {
      \pgfmathsetmacro{\ang}{60*\k+30}
      \draw[bondCHb] ({cos(\ang)*\rIn},{sin(\ang)*\rIn}) -- ({cos(\ang)*\rOut},{sin(\ang)*\rOut});
      \node[Hatomb] at ({cos(\ang)*(\rOut+0.15)},{sin(\ang)*(\rOut+0.15)}) {H};
    }
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
  % v4 -- libres : k=3,4
  \begin{scope}[shift={(3*\apo,-\dyhex)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \foreach \k in {3,4} {
      \pgfmathsetmacro{\ang}{60*\k+30}
      \draw[bondCHb] ({cos(\ang)*\rIn},{sin(\ang)*\rIn}) -- ({cos(\ang)*\rOut},{sin(\ang)*\rOut});
      \node[Hatomb] at ({cos(\ang)*(\rOut+0.15)},{sin(\ang)*(\rOut+0.15)}) {H};
    }
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
  % v5 -- libres : k=0,4,5
  \begin{scope}[shift={(5*\apo,-\dyhex)}]
    \foreach \i in {0,...,5} { \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex}); }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \foreach \k in {0,4,5} {
      \pgfmathsetmacro{\ang}{60*\k+30}
      \draw[bondCHb] ({cos(\ang)*\rIn},{sin(\ang)*\rIn}) -- ({cos(\ang)*\rOut},{sin(\ang)*\rOut});
      \node[Hatomb] at ({cos(\ang)*(\rOut+0.15)},{sin(\ang)*(\rOut+0.15)}) {H};
    }
    \node[Catom] at (P0) {C}; \node[Catom] at (P1) {C}; \node[Catom] at (P2) {C};
    \node[Catom] at (P3) {C}; \node[Catom] at (P4) {C}; \node[Catom] at (P5) {C};
  \end{scope}
\end{tikzpicture}
\caption{Deux exemples de HAP avec leurs hydrogènes explicites : l'azulène
($\mathrm{C_{10}H_8}$), non-benzénoïde formé d'un pentagone et d'un
heptagone accolés (à gauche), et un benzénoïde non linéaire à six
hexagones (à droite), repris en figures~\ref{fig:graphe-carbone}
et~\ref{fig:dual} sous forme de graphe carbone puis de graphe dual.}
\label{fig:azulene}
\end{figure}

\begin{remarque}
Pour alléger la lecture, les figures suivantes ne représenteront plus
explicitement les atomes d'hydrogène ni les liaisons C--H : ces derniers
sont implicites sur tous les carbones de degré $2$ du graphe carbone (les
carbones de bord).
\end{remarque}

\subsection{Objectif et problématique}

L'objectif du stage est le suivant : \emph{étant donné un benzénoïde, énumérer
de manière exhaustive toutes les substitutions admissibles dans lesquelles
chaque hexagone est soit conservé, soit remplacé par un cycle à $5$ ou $7$
atomes}. La difficulté est double, à la fois combinatoire et géométrique. Nous
en donnons l'énoncé précis et l'analyse détaillée à la section~\ref{sec:probleme},
une fois introduit le vocabulaire nécessaire.

\subsection{Positionnement et contributions}

Ce stage se place dans la continuité directe de la thèse de Varet~\cite{varet2022} :
nous reprenons son cadre (modélisation PPC sur le \emph{graphe dual} d'un
benzénoïde, notions d'aromaticité) et nous l'étendons au cas, non traité
jusqu'ici, des cycles à $5$ et $7$ atomes. Nos contributions sont les suivantes.

\begin{itemize}[leftmargin=1.4em]
  \item Un \textbf{modèle PPC} qui énumère exhaustivement les substitutions
    $5/6/7$ d'un benzénoïde, fondé sur une \emph{table de voisinage} encodant
    les configurations locales chimiquement réalisables.
  \item Un \textbf{pipeline de validation physique} qui reconstruit en trois
    dimensions chaque solution du modèle, l'optimise par une méthode quantique
    semi-empirique (\xtb{}), puis juge sa planéité.
  \item Une \textbf{étude expérimentale systématique} de contraintes
    additionnelles visant à augmenter la proportion de molécules planes
    produites. Ce point mérite une précision : la planéité d'une molécule est
    une propriété \emph{globale} qui ne se laisse pas exprimer entièrement par
    des contraintes locales --- c'est une difficulté intrinsèque au problème, et
    non un défaut de modélisation. Notre démarche consiste à identifier
    expérimentalement les contraintes qui, sans capturer la planéité au sens
    strict, en améliorent significativement le rendement.
  \item Une \textbf{extension de l'analyse électronique} (structures de Kekulé,
    couvertures de Clar, \emph{Ring Bond Order}) aux cycles à $5$ et $7$ atomes,
    là où les outils existants se limitent aux hexagones.
\end{itemize}

Le mémoire est organisé comme suit. La section~\ref{sec:prelim} introduit les
deux corpus de notions nécessaires --- la PPC et la chimie des HAP. La
section~\ref{sec:probleme} détaille le problème et ses difficultés. La
section~\ref{sec:modele} présente notre modélisation. La
section~\ref{sec:realisation} décrit la réalisation (reconstruction, validation,
analyse). La section~\ref{sec:xp} rapporte l'étude expérimentale, et la
section~\ref{sec:conclusion} conclut.

% =====================================================================
\section{Préliminaires et état de l'art}
\label{sec:prelim}
% =====================================================================

Notre travail se situe à l'interface de la chimie théorique et de
l'informatique. Les notions nécessaires à la lecture du mémoire sont
d'abord celles de la chimie des HAP, puis le formalisme de la
programmation par contraintes.

\subsection{Notions de chimie}

Les termes de chimie employés dans la suite peuvent être lus aussi bien
comme des définitions sur des graphes que comme le vocabulaire chimique
usuel.

\subsubsection{Définitions préliminaires}
\label{sec:chimie-prelim}

Avant d'introduire les notions spécifiques aux HAP, nous rappelons
brièvement le vocabulaire chimique de base utilisé dans le mémoire. Le
lecteur familier peut sauter cette sous-section.

\underline{Atome et molécule} : un atome est l'unité élémentaire de la
matière chimique ; dans ce travail, seuls deux types interviennent : le
carbone (C) et l'hydrogène (H). Une molécule est un assemblage stable
d'atomes reliés par des liaisons covalentes.

\underline{Liaison covalente, simple et double} : une liaison covalente
résulte du partage d'une ou plusieurs paires d'électrons entre deux
atomes. On parle de liaison \emph{simple} lorsqu'une seule paire est
partagée, de liaison \emph{double} lorsque deux paires le sont.

\underline{Valence} : la valence d'un atome est le nombre de liaisons
qu'il peut former. Le carbone est tétravalent (quatre liaisons),
l'hydrogène monovalent (une liaison).

\underline{Électrons $\sigma$ et $\pi$} : les électrons d'une liaison
covalente se répartissent en deux types selon la géométrie du
recouvrement orbitalaire. Une liaison simple est constituée d'une paire
d'électrons formant une \emph{liaison} $\sigma$, qui appartient au
squelette rigide de la molécule. Une liaison double comporte deux
paires : la première forme également une liaison $\sigma$, la seconde
une \emph{liaison} $\pi$, dont les électrons occupent des orbitales
perpendiculaires au plan local et sont \emph{plus faiblement
localisés}.

\newpage

\subsubsection{HAP, benzénoïdes et graphe dual}
\label{sec:dual}

Les \textbf{hydrocarbures aromatiques polycycliques} (HAP) sont des
molécules formées exclusivement d'atomes de carbone et d'hydrogène, dont
les carbones constituent un assemblage de cycles fusionnés. Lorsque ces
cycles sont exclusivement des hexagones, on parle de \textbf{benzénoïde}
--- le naphtalène ou le pyrène en sont des exemples typiques. Mais
certains HAP comportent aussi des cycles à cinq ou sept atomes : on les
appelle \textbf{non-benzénoïdes}, comme l'azulène
(figure~\ref{fig:azulene}). Ce sont précisément ces structures qui nous
intéressent dans ce travail, en raison de leurs propriétés électroniques
particulières : les électrons $\pi$ introduits à la
section~\ref{sec:chimie-prelim} s'y répartissent et se délocalisent
différemment que dans un pavage hexagonal régulier, ce qui modifie
l'aromaticité et la stabilité de la molécule.

\begin{definition}[Graphe carbone]
À toute molécule HAP $M$ on associe son \textbf{graphe carbone}
$G_C(M) = (V_C, E_C)$ : les sommets $V_C$ sont les atomes de carbone,
et les arêtes $E_C$ représentent les liaisons covalentes
carbone--carbone. Les hydrogènes périphériques, déduits des valences
libres des carbones de bord, n'apparaissent pas dans ce graphe.
L'intérêt de cette représentation est de pouvoir raisonner directement
sur les \textbf{interactions entre atomes de carbone}, indépendamment
des hydrogènes périphériques qui n'apportent pas d'information
structurelle.
\end{definition}

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.55, baseline=(current bounding box.center)]
  \pgfmathsetmacro{\rP}{1.4/(2*sin(36))}
  \pgfmathsetmacro{\hP}{\rP*cos(36)}
  \coordinate (CP) at (-\hP, 0);
  \foreach \i in {0,...,4} {
    \coordinate (P\i) at ($(CP)+({\rP*cos(72*\i+36)},{\rP*sin(72*\i+36)})$);
  }
  \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--cycle;
  \node at (CP) {$0$};
  \pgfmathsetmacro{\rH}{1.4/(2*sin(180/7))}
  \pgfmathsetmacro{\hH}{\rH*cos(180/7)}
  \pgfmathsetmacro{\angA}{180-(180/7)}
  \coordinate (CH) at (\hH, 0);
  \foreach \i in {0,...,6} {
    \coordinate (H\i) at ($(CH)+({\rH*cos(\angA-\i*360/7)},{\rH*sin(\angA-\i*360/7)})$);
  }
  \draw[thick] (H0)--(H1)--(H2)--(H3)--(H4)--(H5)--(H6)--cycle;
  \node at (CH) {$1$};
\end{tikzpicture}
\hspace{1.2cm}
\begin{tikzpicture}[scale=0.55, baseline=(current bounding box.center)]
  \def\rhex{1.2}
  \pgfmathsetmacro{\apo}{\rhex*cos(30)}
  \pgfmathsetmacro{\dyhex}{1.5*\rhex}
  % Rangee centrale v0, v1, v2
  \foreach \k/\lab in {0/v_0, 1/v_1, 2/v_2} {
    \begin{scope}[shift={({\k*2*\apo}, 0)}]
      \foreach \i in {0,...,5} {
        \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex});
      }
      \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
      \node at (0,0) {$\lab$};
    \end{scope}
  }
  % v3 en haut, entre v0 et v1
  \begin{scope}[shift={(\apo, \dyhex)}]
    \foreach \i in {0,...,5} {
      \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex});
    }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \node at (0,0) {$v_3$};
  \end{scope}
  % v4 en bas, entre v1 et v2
  \begin{scope}[shift={(3*\apo, -\dyhex)}]
    \foreach \i in {0,...,5} {
      \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex});
    }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \node at (0,0) {$v_4$};
  \end{scope}
  % v5 en bas-droite, voisin de v4 et v2
  \begin{scope}[shift={(5*\apo, -\dyhex)}]
    \foreach \i in {0,...,5} {
      \coordinate (P\i) at ({cos(60*\i+30)*\rhex}, {sin(60*\i+30)*\rhex});
    }
    \draw[thick] (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \node at (0,0) {$v_5$};
  \end{scope}
\end{tikzpicture}
\caption{Graphe carbone de l'azulène (à gauche, cycles indexés $0$ et $1$)
et d'un benzénoïde non linéaire à six hexagones (à droite,
$v_0, \dots, v_5$) -- même molécules qu'en figure~\ref{fig:azulene},
débarrassées de leurs hydrogènes.}
\label{fig:graphe-carbone}
\end{figure}

Parmi les HAP, les \textbf{benzénoïdes} forment la sous-famille
constituée exclusivement d'hexagones réguliers fusionnés par des arêtes
du pavage hexagonal. Plus généralement, on s'intéressera également aux
HAP comportant des cycles à $5$, $6$ ou $7$ atomes. Pour tout HAP, on
définit son \textbf{graphe dual} $\GD$, qui résume la combinatoire des
cycles indépendamment de leur taille.

\begin{definition}[Graphe dual]
Soit $M$ un HAP formé de $n$ cycles. Son graphe dual $\GD(M) = (V_D, E_D)$
a pour sommets $V_D = \{0, \dots, n-1\}$ (un sommet par cycle), et
$(u, v) \in E_D$ si et seulement si les cycles $u$ et $v$ partagent une
arête carbone--carbone. Cette construction correspond au \emph{graphe
dual intérieur} de la littérature, c'est-à-dire le graphe dual classique
privé de la face externe. L'intérêt de cette représentation est de
pouvoir raisonner directement sur les \textbf{interactions entre
cycles} (partage d'arêtes), sans se soucier du détail atomique interne
à chaque cycle.
\end{definition}

Pour un benzénoïde $B$, on note $n_H(B)$ son nombre d'hexagones ; ainsi
$|V_D(B)| = n_H(B)$. On appelle \textbf{bord} de $\GD$ l'ensemble des
cycles de degré strictement inférieur à $6$, c'est-à-dire ceux qui ne
sont pas entièrement entourés de voisins. La figure~\ref{fig:dual}
illustre le graphe dual du benzénoïde de la
figure~\ref{fig:graphe-carbone}.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.9, baseline=(current bounding box.center)]
  \node[circle,draw,inner sep=2pt] (m0) at (0, 0) {$0$};
  \node[circle,draw,inner sep=2pt] (m1) at (1.4, 0) {$1$};
  \draw (m0)--(m1);
\end{tikzpicture}
\hspace{1.2cm}
\begin{tikzpicture}[scale=0.9, baseline=(current bounding box.center)]
  \node[circle,draw,inner sep=2pt] (n0) at (0, 0) {$0$};
  \node[circle,draw,inner sep=2pt] (n1) at (1.4, 0) {$1$};
  \node[circle,draw,inner sep=2pt] (n2) at (2.8, 0) {$2$};
  \node[circle,draw,inner sep=2pt] (n3) at (0.7, 1.2) {$3$};
  \node[circle,draw,inner sep=2pt] (n4) at (2.1, -1.2) {$4$};
  \node[circle,draw,inner sep=2pt] (n5) at (3.5, -1.2) {$5$};
  \draw (n0)--(n1);
  \draw (n1)--(n2);
  \draw (n0)--(n3);
  \draw (n1)--(n3);
  \draw (n1)--(n4);
  \draw (n2)--(n4);
  \draw (n2)--(n5);
  \draw (n4)--(n5);
\end{tikzpicture}
\caption{Graphe dual de l'azulène (à gauche, $1$ arête entre les cycles
$0$ et $1$) et du benzénoïde de la figure~\ref{fig:graphe-carbone}
(à droite) -- continuité directe avec les graphes carbone de la
figure~\ref{fig:graphe-carbone}.}
\label{fig:dual}
\end{figure}

\subsubsection{Structures de Kekulé}

Une \textbf{structure de Kekulé} est un étiquetage des liaisons carbone--carbone
en simples et doubles tel que chaque carbone porte exactement une double
liaison. En termes de graphes, c'est un \emph{couplage parfait} du graphe
carbone. Une molécule HAP possède en général plusieurs structures de Kekulé,
mais il est aussi possible qu'elle n'en possède aucune lorsque son graphe
carbone n'admet pas de couplage parfait.

L'intérêt de connaître l'ensemble $\mathcal{K}(B)$ des structures de
Kekulé d'une molécule est de pouvoir, par la suite, estimer sa stabilité
et son aromaticité par des moyens purement combinatoires
(section~\ref{sec:rbo}), et de détecter ses sites radicalaires lorsque
$\mathcal{K}(B) = \emptyset$. Mais surtout, une molécule réelle n'adopte
pas une structure de Kekulé unique et figée : à un instant donné, on ne
peut pas prédire laquelle de ses structures décrit effectivement la
molécule, les liaisons doubles se redistribuant en permanence entre les
configurations équivalentes (délocalisation, détaillée en
section~\ref{sec:aromaticite}). C'est précisément pour cette raison
qu'il est nécessaire d'énumérer \emph{toutes} les structures de Kekulé
d'une molécule, plutôt que de n'en retenir qu'une seule.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.9]
  % Coordonnees naphtalene (utilisees pour chaque scope)
  \def\drawNaph{%
    \coordinate (P0) at (0.693, 0.4);
    \coordinate (P1) at (0, 0.8);
    \coordinate (P2) at (-0.693, 0.4);
    \coordinate (P3) at (-0.693, -0.4);
    \coordinate (P4) at (0, -0.8);
    \coordinate (P5) at (0.693, -0.4);
    \coordinate (Q1) at (1.386, 0.8);
    \coordinate (Q0) at (2.079, 0.4);
    \coordinate (Q5) at (2.079, -0.4);
    \coordinate (Q4) at (1.386, -0.8);
    \coordinate (CL) at (0, 0);
    \coordinate (CR) at (1.386, 0);
    \draw (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \draw (P0)--(Q1)--(Q0)--(Q5)--(Q4)--(P5);
  }
  % Macro: double liaison "interieure" -- trait parallele decale vers le centre C
  % Usage : \dbl{A}{B}{C}  ou C est le centre du cycle contenant l'arete AB
  \newcommand{\dbl}[3]{%
    \draw ($(#1)!0.18!(#3)$) -- ($(#2)!0.18!(#3)$);
  }
  % K1 : sextets dans chaque hexagone, oriente avec doubles "externes" alternees
  \begin{scope}[xshift=0cm]
    \drawNaph
    \dbl{P0}{P1}{CL}
    \dbl{P2}{P3}{CL}
    \dbl{P4}{P5}{CL}
    \dbl{Q1}{Q0}{CR}
    \dbl{Q5}{Q4}{CR}
    \node at (1.04, -1.4) {$K_1$};
  \end{scope}
  % K2
  \begin{scope}[xshift=4.0cm]
    \drawNaph
    \dbl{P0}{P5}{CL}
    \dbl{P1}{P2}{CL}
    \dbl{P3}{P4}{CL}
    \dbl{Q1}{Q0}{CR}
    \dbl{Q5}{Q4}{CR}
    \node at (1.04, -1.4) {$K_2$};
  \end{scope}
  % K3
  \begin{scope}[xshift=8.0cm]
    \drawNaph
    \dbl{P0}{Q1}{CR}
    \dbl{P5}{Q4}{CR}
    \dbl{P3}{P4}{CL}
    \dbl{P1}{P2}{CL}
    \dbl{Q0}{Q5}{CR}
    \node at (1.04, -1.4) {$K_3$};
  \end{scope}
\end{tikzpicture}
\caption{Les trois structures de Kekulé du naphtalène.}
\label{fig:kekule-naphtalene}
\end{figure}

\subsubsection{Sextets et couvertures de Clar}

Un \textbf{sextet de Clar} (ou rond de Clar) est un hexagone dont les six
électrons $\pi$ forment un cycle aromatique autonome. Concrètement, on peut
le visualiser comme trois liaisons doubles alternées sur le cycle, et on le
représente conventionnellement par un cercle inscrit dans l'hexagone (voir
figure~\ref{fig:sextet}). La règle de Hückel
(voir annexe~\ref{app:huckel}) impose qu'un sextet occupe exactement six
atomes ($4n+2$ électrons $\pi$, $n=1$) : seuls les \emph{hexagones}
peuvent porter un sextet, jamais un pentagone ni un heptagone.

La clé de la notion de sextet est la \textbf{délocalisation} : les six
électrons $\pi$ d'un sextet ne sont pas figés dans une configuration de
liaisons doubles particulière, ils \emph{circulent librement} autour du
cycle. Autrement dit, la molécule ne possède pas une unique structure de
Kekulé fixe : on ne peut pas prédire à l'avance dans quelle configuration
elle se trouvera à un instant donné --- elle oscille en permanence entre
toutes ses structures de Kekulé équivalentes. C'est précisément cette
mobilité qui explique qu'une même couverture de Clar puisse correspondre
à plusieurs structures de Kekulé.

Une \textbf{couverture de Clar} place des sextets
sur un ensemble d'hexagones deux à deux disjoints, et le \textbf{nombre de
Clar} est le nombre maximal de sextets que l'on peut placer.

L'intérêt du nombre de Clar est qu'il fournit une \textbf{estimation
combinatoire de l'aromaticité} : la couverture qui maximise le nombre de
sextets correspond empiriquement à la structure de résonance la plus
stable, sans qu'il soit nécessaire de résoudre un calcul quantique
\emph{ab initio} coûteux. C'est cet avantage --- remplacer un calcul
quantique par un dénombrement sur le graphe --- que nous exploitons
systématiquement dans ce travail, à l'instar de Varet~\cite{varet2022}.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.9]
  % Hexagone "trois doubles alternees" a gauche
  \begin{scope}[xshift=0cm]
    \foreach \i in {0,...,5} {
      \coordinate (S\i) at ({cos(60*\i+30)*0.8}, {sin(60*\i+30)*0.8});
    }
    \coordinate (CS) at (0,0);
    \draw (S0)--(S1)--(S2)--(S3)--(S4)--(S5)--cycle;
    \newcommand{\dbs}[3]{\draw ($(#1)!0.18!(#3)$) -- ($(#2)!0.18!(#3)$);}
    \dbs{S0}{S1}{CS}
    \dbs{S2}{S3}{CS}
    \dbs{S4}{S5}{CS}
  \end{scope}
  % Fleche
  \draw[->, thick] (1.1, 0) -- (2.0, 0);
  % Hexagone avec cercle (representation conventionnelle du sextet)
  \begin{scope}[xshift=3.1cm]
    \foreach \i in {0,...,5} {
      \coordinate (T\i) at ({cos(60*\i+30)*0.8}, {sin(60*\i+30)*0.8});
    }
    \draw (T0)--(T1)--(T2)--(T3)--(T4)--(T5)--cycle;
    \draw[thick] (0,0) circle (0.55);
  \end{scope}
\end{tikzpicture}
\caption{Représentation d'un sextet de Clar.}
\label{fig:sextet}
\end{figure}

Une même couverture de Clar peut correspondre à plusieurs structures de
Kekulé : sur le naphtalène (figure~\ref{fig:clar-naphtalene}), les
Kekulé $K_1$ et $K_2$ ne diffèrent que par la rotation des liaisons
doubles à l'intérieur de l'hexagone gauche et correspondent toutes deux
à la couverture $C_A$ (sextet sur l'hexagone gauche). $K_3$ correspond
à la couverture $C_B$ (sextet sur l'hexagone droit). Le nombre de Clar
du naphtalène vaut donc $1$ : on ne peut placer qu'un seul sextet à la
fois, les deux hexagones partageant une arête.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.85]
  \def\drawNaph{%
    \coordinate (P0) at (0.693, 0.4);
    \coordinate (P1) at (0, 0.8);
    \coordinate (P2) at (-0.693, 0.4);
    \coordinate (P3) at (-0.693, -0.4);
    \coordinate (P4) at (0, -0.8);
    \coordinate (P5) at (0.693, -0.4);
    \coordinate (Q1) at (1.386, 0.8);
    \coordinate (Q0) at (2.079, 0.4);
    \coordinate (Q5) at (2.079, -0.4);
    \coordinate (Q4) at (1.386, -0.8);
    \coordinate (CL) at (0, 0);
    \coordinate (CR) at (1.386, 0);
    \draw (P0)--(P1)--(P2)--(P3)--(P4)--(P5)--cycle;
    \draw (P0)--(Q1)--(Q0)--(Q5)--(Q4)--(P5);
  }
  \newcommand{\dbn}[3]{\draw ($(#1)!0.18!(#3)$) -- ($(#2)!0.18!(#3)$);}

  % K1 (haut gauche)
  \begin{scope}[shift={(0, 4)}]
    \drawNaph
    \dbn{P0}{P1}{CL}\dbn{P2}{P3}{CL}\dbn{P4}{P5}{CL}
    \dbn{Q1}{Q0}{CR}\dbn{Q5}{Q4}{CR}
    \node at (1.04, -1.35) {$K_1$};
  \end{scope}
  % K2 (haut milieu)
  \begin{scope}[shift={(4.2, 4)}]
    \drawNaph
    \dbn{P0}{P5}{CL}\dbn{P1}{P2}{CL}\dbn{P3}{P4}{CL}
    \dbn{Q1}{Q0}{CR}\dbn{Q5}{Q4}{CR}
    \node at (1.04, -1.35) {$K_2$};
  \end{scope}
  % K3 (haut droite)
  \begin{scope}[shift={(9.5, 4)}]
    \drawNaph
    \dbn{P0}{Q1}{CR}\dbn{P5}{Q4}{CR}\dbn{Q0}{Q5}{CR}
    \dbn{P1}{P2}{CL}\dbn{P3}{P4}{CL}
    \node at (1.04, -1.35) {$K_3$};
  \end{scope}

  % CA (bas gauche)
  \begin{scope}[shift={(2.1, 0)}]
    \drawNaph
    \draw[thick] (0,0) circle (0.45);
    \dbn{Q1}{Q0}{CR}\dbn{Q5}{Q4}{CR}
    \node at (1.04, -1.35) {$C_A$};
  \end{scope}
  % CB (bas droite)
  \begin{scope}[shift={(9.5, 0)}]
    \drawNaph
    \draw[thick] (1.386,0) circle (0.45);
    \dbn{P1}{P2}{CL}\dbn{P3}{P4}{CL}
    \node at (1.04, -1.35) {$C_B$};
  \end{scope}

  % Fleches K1 vers CA et K2 vers CA
  \draw[->, thick] (0.80, 2.5) -- (1.8, 1.3);
  \draw[->, thick] (5.24, 2.5) -- (4, 1.3);
  % Fleche K3 vers CB
  \draw[->, thick] (10.20, 2.5) -- (10.20, 1.3);
\end{tikzpicture}
\caption{Les deux couvertures de Clar du naphtalène}
\label{fig:clar-naphtalene}
\end{figure}

\subsubsection{Ring Bond Order (RBO)}
\label{sec:rbo}

Le \emph{ring bond order} (RBO) est une mesure combinatoire de la
délocalisation aromatique locale d'un hexagone au sein d'un benzénoïde.
Il prolonge le concept de \emph{bond order} introduit par
Pauling~\cite{pauling} à partir de la théorie de la résonance, formalisé
ensuite comme indicateur cyclique notamment par
Randić~\cite{randic}. Nous reprenons ici les notations de
Varet~\cite{varet2022}.

Comme le nombre de Clar, le RBO permet d'estimer la délocalisation
aromatique sans calcul quantique coûteux, mais à une granularité plus
fine : il quantifie la délocalisation \emph{par cycle}, et non plus de
façon binaire (sextet ou non) à l'échelle de la molécule entière. C'est
cette finesse qui nous permettra de comparer des cycles de tailles
différentes (pentagones, hexagones, heptagones).

Soit $B = (V, E)$ le graphe carbone d'un benzénoïde : $V$ désigne
l'ensemble des atomes de carbone et $E$ celui des liaisons covalentes
entre atomes voisins. On note $\mathcal{K}(B)$ l'ensemble de ses
structures de Kekulé, c'est-à-dire des couplages parfaits du graphe.
Lorsque $\mathcal{K}(B) \neq \emptyset$, on dit que $B$ est
\emph{Kekuléen} ; dans le cas contraire ($\mathcal{K}(B) = \emptyset$),
la molécule est dite \emph{non-Kekuléenne} et admet au moins un
\emph{site radicalaire} (voir figure~\ref{fig:radical-5-6} et
section~\ref{sec:radicals}).

\begin{definition}[Bond order, \cite{varet2022}]
Soient $B = (V, E)$ un benzénoïde Kekuléen et $e \in E$ une de ses
liaisons. Le \emph{bond order} de $e$ est le rapport entre le nombre de
structures de Kekulé dans lesquelles $e$ apparaît comme liaison double
et le nombre total de structures de Kekulé de $B$ :
\[
  \mathrm{bo}(e) \;=\;
  \frac{\bigl|\{\, K \in \mathcal{K}(B) \,:\, e \in K \,\}\bigr|}
       {\bigl|\mathcal{K}(B)\bigr|} .
\]
\end{definition}

C'est une mesure de la délocalisation locale : $\mathrm{bo}(e) = 1$
signifie que $e$ est double dans toutes les Kekulé (liaison localisée),
$\mathrm{bo}(e) = 0$ qu'elle est toujours simple, et toute valeur
intermédiaire traduit une délocalisation partielle.

\begin{definition}[Ring bond order, \cite{varet2022}]
Soient $B$ un benzénoïde Kekuléen et $h$ un de ses hexagones. Le
\emph{ring bond order} de $h$ est la somme des bond order associés aux
six liaisons définissant $h$ :
\[
  \mathrm{RBO}(h) \;=\; \sum_{e \in E(h)} \mathrm{bo}(e) ,
\]
où $E(h) \subset E$ désigne l'ensemble des six arêtes du cycle $h$.
\end{definition}

\begin{remarque}
Pour tout hexagone $h$ d'un benzénoïde Kekuléen, on a
$0 \le \mathrm{RBO}(h) \le 3$. La borne supérieure provient du fait que,
dans toute structure de Kekulé $K \in \mathcal{K}(B)$, au plus
\emph{trois} arêtes de $h$ peuvent être doubles dans $K$ (un couplage
parfait restreint aux six sommets de $h$ comporte au plus trois arêtes).
En sommant cette inégalité sur les structures et en divisant par
$|\mathcal{K}(B)|$, on obtient $\mathrm{RBO}(h) \le 3$.
\end{remarque}

À titre d'exemple, le benzène admet exactement deux structures de
Kekulé, échangées par rotation des liaisons doubles. Chaque arête y est
double dans une seule des deux structures : $\mathrm{bo}(e) = 1/2$ pour
les six arêtes, et l'unique hexagone vérifie
$\mathrm{RBO}(h) = 6 \times 1/2 = 3$, la valeur maximale. Le
naphtalène, qui admet trois structures de Kekulé
(figure~\ref{fig:kekule-naphtalene}), voit chacun de ses deux hexagones
atteindre $\mathrm{RBO}(h) = 8/3 \approx 2{,}67$ : cette valeur
strictement inférieure à $3$ traduit la délocalisation partielle propre
aux benzénoïdes polycycliques.

\paragraph{Extension aux cycles de taille quelconque.}
Les molécules considérées dans ce travail comportent non seulement des
hexagones, mais aussi des cycles à cinq et sept atomes. Pour traiter ces
cas, nous étendons la définition précédente. Soit $C$ un cycle de
taille $n$ d'un graphe moléculaire Kekuléen, et $E(C)$ l'ensemble de ses
$n$ arêtes. On définit le \emph{cycle bond order}
\[
  \mathrm{CBO}(C) \;=\; \sum_{e \in E(C)} \mathrm{bo}(e) .
\]
Le même argument que ci-dessus donne l'encadrement
\[
  0 \;\le\; \mathrm{CBO}(C) \;\le\; \lfloor n/2 \rfloor ,
\]
puisqu'un cycle de longueur $n$ ne peut porter, dans une structure de
Kekulé donnée, plus de $\lfloor n/2 \rfloor$ arêtes doubles. Pour
comparer la délocalisation entre cycles de tailles différentes, nous
reportons systématiquement le ratio normalisé
$\mathrm{CBO}(C) / \lfloor n/2 \rfloor \in [0, 1]$, qui vaut $1$ pour
un cycle « parfaitement aromatique » au sens combinatoire. Dans le cas
radicalaire où $\mathcal{K}(B) = \emptyset$, les quantités
$\mathrm{bo}(e)$, $\mathrm{RBO}(h)$ et $\mathrm{CBO}(C)$ ne sont pas
définies au sens classique.

\subsubsection{Sites radicalaires}
\label{sec:radicals}

Lorsqu'une molécule n'admet aucun couplage parfait (par exemple à cause d'un
nombre impair d'atomes ou d'une topologie particulière), certains carbones
restent non couverts : ils portent un \textbf{électron $\pi$ non apparié},
appelé aussi \emph{électron célibataire}. La molécule devient dans ce cas
un (mono)radical. La parité impaire des cycles à $5$ et $7$ atomes favorise
précisément l'apparition de tels sites, ce qui rend les structures
non-benzénoïdes intéressantes pour la chimie radicalaire.

L'exemple le plus simple est celui d'un pentagone accolé à un hexagone
(figure~\ref{fig:radical-5-6}) : le squelette compte $5 + 6 - 2 = 9$ atomes
de carbone, un nombre impair. Aucun couplage parfait ne peut donc exister, et
au moins un carbone reste non couvert dans toute structure de Kekulé : un
site radicalaire est \emph{forcé} par la topologie.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.0]
  % Hexagone a gauche : sommets en orientation "pointe haut/bas", centre (0,0)
  \coordinate (H0) at (0.693, 0.4);
  \coordinate (H1) at (0, 0.8);
  \coordinate (H2) at (-0.693, 0.4);
  \coordinate (H3) at (-0.693, -0.4);
  \coordinate (H4) at (0, -0.8);
  \coordinate (H5) at (0.693, -0.4);
  % Pentagone a droite, partageant l'arete H0-H5 (verticale)
  % Construction du pentagone regulier passant par H0 et H5
  % rayon r=0.8/(2 sin(36)) approx 0.6805, centre a droite a une distance
  % h = r cos(36) approx 0.5505
  \coordinate (Q0) at ($(0.693, 0) + (0.5505, 0)$);     % centre du pentagone
  % Construction explicite via coordonnees calculees
  \coordinate (R0) at (0.693, 0.4);     % = H0 (sommet partage haut)
  \coordinate (R4) at (0.693, -0.4);    % = H5 (sommet partage bas)
  % les 3 autres sommets du pentagone, par rotation de 72deg autour de Q0
  \coordinate (R1) at ($(Q0) + ({0.6805*cos(54)}, {0.6805*sin(54)})$);
  \coordinate (R2) at ($(Q0) + ({0.6805*cos(-18)}, {0.6805*sin(-18)})$);
  \coordinate (R3) at ($(Q0) + ({0.6805*cos(-90)}, {0.6805*sin(-90)})$);
  % On dessine R1 en haut a droite mais l'ordre cyclique est R0 R1 R2 R3 R4
  \coordinate (CL) at (0,0);
  \coordinate (CR) at (Q0);
  % Trace
  \draw (H0)--(H1)--(H2)--(H3)--(H4)--(H5)--cycle;
  \draw (R0) -- ($(Q0)+({0.6805*cos(54)},{0.6805*sin(54)})$)
        -- ($(Q0)+({0.6805*cos(-18)},{0.6805*sin(-18)})$)
        -- ($(Q0)+({0.6805*cos(-90)},{0.6805*sin(-90)})$)
        -- (R4);
  % Doubles liaisons (style interieur), un Kekule possible :
  % Hex : H1-H2, H3-H4 doubles ; H0,H5 restent pour le partage
  \newcommand{\dblr}[3]{\draw ($(#1)!0.18!(#3)$) -- ($(#2)!0.18!(#3)$);}
  \dblr{H1}{H2}{CL}
  \dblr{H3}{H4}{CL}
  \dblr{H0}{H5}{CL}
  % Pentagone : on met une double entre R1 et le sommet a -18deg
  \coordinate (RA) at ($(Q0)+({0.6805*cos(54)},{0.6805*sin(54)})$);
  \coordinate (RB) at ($(Q0)+({0.6805*cos(-18)},{0.6805*sin(-18)})$);
  \coordinate (RC) at ($(Q0)+({0.6805*cos(-90)},{0.6805*sin(-90)})$);
  \dblr{RA}{RB}{CR}
  % Le sommet RC reste sans double : site radicalaire
  \fill (RC) circle (1.6pt);
  \node[anchor=west, font=\scriptsize, inner sep=1.5pt] at (RC) {\;(radical)};
\end{tikzpicture}
\caption{Un pentagone accolé à un hexagone}
\label{fig:radical-5-6}
\end{figure}

\subsubsection{Aromaticité et délocalisation}
\label{sec:aromaticite}

L'\textbf{aromaticité} traduit la stabilité particulière de certains
cycles conjugués. La \emph{règle de Hückel} (annexe~\ref{app:huckel})
en donne le critère classique : un cycle est dit aromatique s'il est
plan, conjugué, et contient $4n+2$ électrons $\pi$ pour un entier $n
\ge 0$ --- le benzène ($n=1$, $6$ électrons) en est l'exemple
fondamental. Dans un tel cycle, les électrons $\pi$ sont
\emph{délocalisés} et circulent autour du cycle. Plus une molécule
admet de représentations équivalentes de ses liaisons doubles (au sens
des structures de Kekulé introduites plus haut), plus la délocalisation
est étendue, et plus la molécule est stable.

L'aromaticité « réelle » d'une molécule se calcule en toute rigueur par
un calcul quantique \emph{ab initio}, prohibitif sur de grandes séries
de molécules. Les structures de Kekulé, le nombre de Clar et le RBO
introduits dans cette section en sont autant d'\textbf{estimations
combinatoires}, calculables instantanément sur le graphe carbone seul :
c'est cette approche, déjà adoptée par Varet~\cite{varet2022} pour les
benzénoïdes, que nous étendons dans ce travail aux cycles à $5$ et $7$
atomes.

\subsubsection{Planéité et courbure}

À toute molécule on associe une fonction \emph{énergie potentielle} qui dépend
de la position de ses atomes ; cette énergie est minimale lorsque les longueurs
et les angles de liaison correspondent aux préférences de chaque atome et que
les répulsions stériques sont relâchées. Une configuration des atomes est dite
à l'\textbf{équilibre énergétique} lorsqu'elle réalise un minimum local de
cette énergie ; c'est dans cette configuration que la molécule existe
effectivement. On dit alors qu'une molécule est \textbf{plane} si tous ses
atomes se placent, à l'équilibre énergétique, dans un même plan.

Pour un pavage d'hexagones réguliers, la somme des angles autour de chaque
sommet vaut $360^\circ$ et la structure est plane. Un pentagone (angle
interne $108^\circ$) introduit localement un \emph{défaut d'angle} par
rapport à un hexagone et la tension de cycle est plus importante : la
molécule tend à se refermer, comme dans un fullerène. À l'inverse, dans un
heptagone ($\approx 128{,}6^\circ$), les angles sont plus ouverts que ceux
d'un hexagone et la molécule tend à être plus flexible, donc à onduler.

\subsubsection{Validation quantique semi-empirique (\xtb{})}

Pour juger si une molécule est réellement plane, on cherche sa géométrie
d'équilibre : la disposition des atomes qui minimise l'énergie. Nous utilisons
une méthode \emph{semi-empirique}, GFN2-\xtb{}~\cite{xtb}, qui approxime
l'énergie à un coût permettant de traiter de grandes séries de molécules et
fournit une géométrie optimisée exploitable à grande échelle. C'est le sens de
la « validation quantique semi-empirique » du titre.

\subsection{Programmation par contraintes (PPC)}
\label{sec:ppc}

La \emph{programmation par contraintes} est un paradigme déclaratif de
résolution de problèmes combinatoires. Plutôt que de décrire
\emph{comment} résoudre un problème, on se contente d'en \emph{décrire
les solutions}, puis on confie la recherche à un solveur générique.

\subsubsection{Réseau de contraintes}

Une instance d'un problème de satisfaction de contraintes (CSP) est
définie par un ensemble de \emph{variables}, chacune dotée d'un
\emph{domaine} de valeurs possibles, et d'un ensemble de
\emph{contraintes} restreignant les combinaisons de valeurs autorisées.
Formellement~\cite{varet2022} :

\begin{definition}[Instance CSP]
Une instance d'un problème de satisfaction de contraintes est un triplet
$P = (X, D, C)$ où :
\begin{itemize}
  \item $X = \{x_1, x_2, \dots, x_n\}$ est un ensemble de $n$
    \emph{variables} ;
  \item $D = \{d_{x_1}, d_{x_2}, \dots, d_{x_n}\}$ est un ensemble de
    \emph{domaines finis} ; la variable $x_i$ prend ses valeurs dans
    le domaine $d_{x_i}$ ;
  \item $C = \{c_1, c_2, \dots, c_m\}$ est un ensemble de $m$
    \emph{contraintes}. Chaque contrainte $c$ est caractérisée par un
    couple $(S(c), R(c))$ où
    \begin{enumerate}
      \item $S(c) = \{x_{i_1}, x_{i_2}, \dots, x_{i_q}\} \subseteq X$
        est la \emph{portée} de $c$, c'est-à-dire l'ensemble des
        variables sur lesquelles porte la contrainte ;
      \item $R(c) \subseteq \displaystyle\prod_{k=1}^{q} d_{x_{i_k}}$
        est sa \emph{relation de compatibilité}, c'est-à-dire l'ensemble
        des combinaisons de valeurs autorisées par la contrainte.
    \end{enumerate}
\end{itemize}
\end{definition}

Une contrainte portant sur une seule variable est dite \emph{unaire},
sur deux variables \emph{binaire}, sur $q$ variables $q$-\emph{aire}.

\subsubsection{Un exemple : le Sudoku}
\label{sec:ppc-exemple}

Pour illustrer le formalisme, considérons le \textbf{Sudoku} : sur une
grille $9 \times 9$ partiellement remplie de chiffres de $1$ à $9$
(figure~\ref{fig:sudoku}), on cherche à compléter les cases vides de
telle sorte que chaque chiffre apparaisse exactement une fois sur
chaque ligne, chaque colonne et chacun des neuf sous-blocs
$3 \times 3$. Les chiffres initialement présents sont appelés
\emph{indices}.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.63, baseline=(current bounding box.center)]
  \draw[thick] (0,0) rectangle (9,9);
  \foreach \i in {1,...,8} {
    \draw (\i,0)--(\i,9);
    \draw (0,\i)--(9,\i);
  }
  \foreach \i in {3,6} {
    \draw[thick] (\i,0)--(\i,9);
    \draw[thick] (0,\i)--(9,\i);
  }
  \foreach \x/\y/\v in {
    0/8/5, 1/8/3, 4/8/7,
    0/7/6, 3/7/1, 4/7/9, 5/7/5,
    1/6/9, 2/6/8, 7/6/6,
    0/5/8, 4/5/6, 8/5/3,
    0/4/4, 3/4/8, 5/4/3, 8/4/1,
    0/3/7, 4/3/2, 8/3/6,
    1/2/6, 6/2/2, 7/2/8,
    3/1/4, 4/1/1, 5/1/9, 8/1/5,
    4/0/8, 7/0/7, 8/0/9
  } {
    \node[font=\large] at (\x+0.5, \y+0.5) {\v};
  }
\end{tikzpicture}
\hspace{0.6cm}
\begin{tikzpicture}[scale=0.63, baseline=(current bounding box.center)]
  \draw[thick] (0,0) rectangle (9,9);
  \foreach \i in {1,...,8} {
    \draw (\i,0)--(\i,9);
    \draw (0,\i)--(9,\i);
  }
  \foreach \i in {3,6} {
    \draw[thick] (\i,0)--(\i,9);
    \draw[thick] (0,\i)--(9,\i);
  }
  \fill[gray!30] (3,4) rectangle (4,5);
  \node[font=\Large] at (3.5,4.5) {$x_{ij}$};
  \draw[red, thick] (0,4) rectangle (9,5);
  \draw[red, thick] (3,0) rectangle (4,9);
  \draw[green!60!black, thick] (3,3) rectangle (6,6);
\end{tikzpicture}
\caption{Un Sudoku partiellement rempli (à gauche). À droite, la valeur
de la case $x_{ij}$ doit différer de toutes les autres cases de sa
ligne, de sa colonne et de son bloc $3 \times 3$.}
\label{fig:sudoku}
\end{figure}

Le problème se modélise comme un CSP $P = (X, D, C)$ :
\begin{itemize}
  \item $X = \{x_1, x_2, \dots, x_{81}\}$ : une variable par case de la
    grille, numérotées de gauche à droite et de haut en bas. La valeur
    de $x_i$ représente le chiffre inscrit dans la $i$-ème case.
  \item $D = \{d_{x_1}, d_{x_2}, \dots, d_{x_{81}}\}$ : pour les cases
    vides, $d_{x_i} = \{1, 2, \dots, 9\}$ ; pour les cases pré-remplies
    de valeur $v$, $d_{x_i} = \{v\}$. Par exemple, sur la grille de la
    figure~\ref{fig:sudoku}, on aurait
    $d_{x_1} = \{2\}$, $d_{x_2} = \{4\}$, $d_{x_3} = \{1\}$,
    $d_{x_4} = \{1, 2, \dots, 9\}$, $\ldots$
  \item $C = \{c_{i,j} \;:\; x_i, x_j \text{ partagent une ligne, une
    colonne ou un bloc } 3 \times 3\}$, où, $\forall (x_i, x_j)$ dans
    cet ensemble, la contrainte binaire $c_{i,j}$ exprime la différence
    des valeurs :
    \[
      S(c_{i,j}) = \{x_i, x_j\}, \qquad
      R(c_{i,j}) = \{(v_i, v_j) \in d_{x_i} \times d_{x_j}
        \mid v_i \neq v_j\}.
    \]
\end{itemize}

\begin{remarque}
Ces contraintes binaires seront avantageusement regroupées sous une
contrainte globale plus compacte à la
section~\ref{sec:ppc-globales}, une fois introduite la notion de
contrainte globale.
\end{remarque}

\subsubsection{Résolution : propagation et recherche arborescente}

\begin{definition}[Solution]
Une \emph{solution} d'une instance CSP $P = (X, D, C)$ est une
affectation à chaque variable $x_i$ d'une valeur de son domaine
$d_{x_i}$ telle que toutes les contraintes de $C$ soient satisfaites.
On parle d'\emph{affectation complète cohérente}. Un problème est dit
\emph{cohérent} s'il possède au moins une solution, et
\emph{incohérent} sinon.
\end{definition}

Décider si une instance CSP admet une solution est un problème
\textbf{NP-complet} dans le cas général : aucun algorithme polynomial
n'est connu pour le résoudre. Les solveurs CSP combinent en pratique
deux mécanismes complémentaires que nous décrivons ci-dessous.

\textbf{Propagation de contraintes} (\textbf{filtrage})\textbf{.}
À chaque étape de la résolution, on retire des domaines les valeurs
qui ne peuvent appartenir à aucune
solution. Le critère le plus courant est la \emph{cohérence d'arc} :
pour toute contrainte $c$ et toute variable $x_i \in S(c)$, on retire
de $d_{x_i}$ toute valeur $v$ qui n'a aucun \emph{support} dans les
domaines des autres variables de $c$ (c'est-à-dire telle qu'aucune
affectation des autres variables ne satisfasse $c$ avec $v$). Sur le
Sudoku, si une case d'une ligne contient $7$, la propagation retire
$7$ du domaine de toutes les autres cases de la même ligne, de la même
colonne et du même bloc.

\textbf{Recherche arborescente.} On affecte les variables une à une.
À chaque nœud, le solveur choisit une variable et lui assigne une
valeur de son domaine courant, puis propage. Si un domaine devient
vide, le solveur \emph{revient en arrière} (\emph{backtrack}) sur le
dernier choix et essaie une autre valeur. La recherche aboutit soit à
une affectation complète cohérente (solution), soit à l'épuisement de
toutes les branches (instance incohérente).

L'intérêt majeur du paradigme est qu'il n'est pas nécessaire d'écrire
un algorithme spécifique à chaque problème : il suffit de formuler le
modèle, puis d'invoquer un solveur traité comme une \emph{boîte
noire}. Dans ce travail, nous écrivons les modèles avec la
bibliothèque Python \textsc{PyCSP3} et nous les résolvons avec le
solveur \ace{} (\emph{Abscon Constraint Engine})~\cite{pycsp3ace}, qui
sait énumérer l'ensemble des solutions d'une instance.

\subsubsection{Contraintes en extension et contraintes globales}
\label{sec:ppc-globales}

La possibilité de spécifier des contraintes de façon modulaire fait la
force de la PPC. On distingue deux grandes familles particulièrement
utiles dans la suite.

Une \textbf{contrainte en extension} (ou \emph{de table}) liste
explicitement les combinaisons de valeurs autorisées (ou interdites)
pour sa portée : c'est exactement la relation $R(c)$ donnée plus haut.
C'est la forme naturelle pour encoder un catalogue de configurations
admissibles, et c'est elle que nous utiliserons pour la \emph{table de
voisinage} chimique (section~\ref{sec:modele}).

Une \textbf{contrainte globale} capture un motif récurrent sur un
nombre arbitraire de variables, avec un \emph{propagateur} spécialisé
plus efficace que la décomposition en contraintes binaires. L'exemple
typique est $\mathit{allDifferent}(x_{i_1}, \dots, x_{i_q})$,
qui généralise les contraintes binaires de différence $c_{i,j}$
de notre exemple du Sudoku (section~\ref{sec:ppc-exemple}) : sa
portée est $S(c) = \{x_{i_1}, \dots, x_{i_q}\}$ et sa relation
\[
  R(c) = \bigl\{\, (v_{i_1}, \dots, v_{i_q}) \in d_{x_{i_1}} \times
    \dots \times d_{x_{i_q}} \;\bigm|\; v_{i_j} \ne v_{i_k}
    \text{ pour } j \ne k \,\bigr\}.
\]
Cette contrainte est équivalente à $\binom{q}{2}$ contraintes binaires
de différence, mais son propagateur dédié élague l'espace de recherche
de manière nettement plus efficace.

\paragraph{Reformulation du Sudoku avec \texttt{allDifferent}.}
Les contraintes binaires $c_{i,j}$ du modèle précédent peuvent être
factorisées par lignes, colonnes et blocs. Pour chaque ligne $L_k$, on
remplace les $\binom{9}{2} = 36$ contraintes binaires de différence par
une seule contrainte globale $\mathit{allDifferent}(x_i : x_i \in L_k)$ ;
de même pour les colonnes et les blocs $3 \times 3$. On obtient ainsi
un modèle équivalent avec $27$ contraintes globales au lieu de plus de
huit cents contraintes binaires, et la propagation est sensiblement
plus efficace.

\paragraph{Illustration de la résolution.}
Reprenons la grille de Sudoku partiellement remplie de la
figure~\ref{fig:sudoku}. Au départ, la
case $x_1$ est connue : $d_{x_1} = \{2\}$. La propagation des
contraintes $\mathit{allDifferent}$ de la ligne $L_1$, de la colonne
$C_1$ et du bloc $R_1$ contenant $x_1$ retire alors $2$ de tous les
domaines des autres cases concernées. Si après propagation un domaine
se réduit à un singleton, l'affectation correspondante est forcée et
déclenche à son tour de nouvelles propagations. Lorsque la propagation
seule ne suffit plus à compléter la grille, le solveur entre en
recherche : il choisit une variable au domaine non encore singleton
(par exemple $x_4$ avec $d_{x_4} = \{1, 5, 7\}$), affecte la première
valeur ($x_4 = 1$) et propage à nouveau. Si à un moment un domaine
devient vide, la contradiction force un retour arrière et le solveur
essaie la valeur suivante ($x_4 = 5$). La recherche aboutit soit à
une affectation complète et cohérente — une solution — soit à
l'épuisement de toutes les branches, ce qui signifie que l'instance
est incohérente.

La souplesse de la PPC, qui consiste à activer, désactiver ou ajouter
une contrainte puis ré-énumérer rapidement les solutions, est un atout
décisif pour un travail mené en dialogue continu avec les chimistes.

\subsubsection{Isomorphisme de graphes}
\label{sec:ppc-isomorphisme}

Comme une partie centrale de notre modélisation repose sur les
automorphismes d'un graphe (rupture de symétrie en
section~\ref{sec:c6}), nous rappelons ici la notion
d'\emph{isomorphisme}, qui apparaît dans tout le mémoire.

\begin{definition}[Isomorphisme de graphes]
Deux graphes non orientés $G = (V_G, E_G)$ et $H = (V_H, E_H)$ sont
dits \textbf{isomorphes} s'il existe une bijection $f : V_G \to V_H$
qui préserve les arêtes, c'est-à-dire telle que
\[
  \forall u, v \in V_G,\quad \{u, v\} \in E_G \iff \{f(u), f(v)\} \in E_H .
\]
La bijection $f$ est alors appelée un \textbf{isomorphisme} entre $G$
et $H$. Si $G = H$, on parle d'\textbf{automorphisme} de $G$ : c'est
une permutation des sommets qui préserve les arêtes.
\end{definition}

Intuitivement, deux graphes isomorphes sont « les mêmes » à un
renommage des sommets près ; ils représentent la même structure
abstraite. Pour notre problème, deux substitutions d'un benzénoïde qui
diffèrent uniquement par un automorphisme du graphe dual correspondent
à la même molécule, et seront comptées une seule fois. La rupture de
symétrie (section~\ref{sec:c6}) traite explicitement ce point.

% =====================================================================
\section{Le problème}
\label{sec:probleme}
% =====================================================================

Maintenant que le vocabulaire est posé, nous pouvons énoncer précisément le
problème et analyser ses deux sources de difficulté.

On part d'un benzénoïde $B$ à $n_H(B)$ hexagones, représenté par son
graphe dual \GD{}. Une \textbf{substitution} affecte à chaque hexagone
$v$ une taille de cycle $x_v \in \{5, 6, 7\}$ : conserver l'hexagone
($x_v = 6$), le contracter en pentagone ($x_v = 5$) ou le dilater en
heptagone ($x_v = 7$). La substitution doit satisfaire les deux contraintes suivantes :
\begin{itemize}[leftmargin=1.4em]
  \item \textbf{conservation du graphe dual} : les arêtes partagées entre
    cycles voisins sont préservées, donc \GD{} reste identique avant et
    après transformation. C'est un point essentiel de la modélisation :
    elle garantit que chaque cycle de la solution correspond
    \emph{exactement} à un hexagone du benzénoïde de départ, ce qui sera
    exploité pour exprimer simplement la conservation des atomes en
    section~\ref{sec:c1} ;
  \item \textbf{conservation du nombre total d'atomes de carbone}.
\end{itemize}

On cherche à \textbf{énumérer toutes les substitutions admissibles},
c'est-à-dire toutes celles qui correspondent à une molécule
chimiquement et géométriquement réalisable, à \emph{isomorphisme}
près : deux substitutions identiques à une symétrie du benzénoïde de
départ décrivent la même molécule et ne sont comptées qu'une seule
fois (la notion d'isomorphisme est définie en
section~\ref{sec:ppc-isomorphisme}).

\subsection{La difficulté combinatoire}

Avec trois valeurs possibles par hexagone, un benzénoïde à $n_H(B)$
hexagones admet a priori $3^{n_H(B)}$ substitutions. Ce nombre croît
exponentiellement, et la grande majorité de ces affectations ne
correspondent à \emph{aucune} molécule valide : elles violent la
conservation du carbone, ou décrivent des voisinages localement
impossibles à reconstruire. Énumérer naïvement puis filtrer est donc
hors de portée dès que $n_H(B)$ dépasse quelques unités. C'est exactement le type de
problème fortement combinatoire pour lequel la PPC est conçue : on encode les
conditions d'admissibilité comme des contraintes, et le solveur n'explore que
les affectations cohérentes.

\subsection{La difficulté géométrique et topologique}

La seconde difficulté est de nature différente, et c'est la plus profonde. Dans
un benzénoïde, tous les cycles sont des hexagones réguliers d'angle interne
$120^\circ$, et le pavage est plan par construction. Dès que l'on introduit des
pentagones (angle interne $108^\circ$) et des heptagones (environ $128{,}6^\circ$),
plusieurs grandeurs cessent d'être fixées :

\begin{itemize}[leftmargin=1.4em]
  \item les \textbf{angles} aux sommets ne se complètent plus à $360^\circ$, ce
    qui crée une courbure locale ;
  \item les \textbf{distances} inter-atomiques et les longueurs de liaison
    s'ajustent pour relâcher les tensions ;
  \item la \textbf{topologie} de fusion impose que les cycles voisins partagent
    exactement une arête, ce qui n'est pas toujours réalisable selon la
    disposition des voisins ;
  \item la \textbf{planéité} qui en résulte n'est pas garantie : la molécule peut
    se courber ou onduler pour minimiser son énergie.
\end{itemize}

Le point crucial est que la planéité est une propriété \emph{globale} : elle
dépend de l'agencement de tous les défauts à la fois, et ne se déduit pas
seulement des voisinages locaux. On peut filtrer localement les configurations
manifestement irréalisables, mais on ne peut pas, par des contraintes locales
seules, garantir qu'une molécule entière sera plane. Cette limite est
\emph{intrinsèque au problème}. Elle motive l'architecture en deux temps de notre
travail : un modèle PPC qui énumère les candidats en appliquant les conditions
\emph{exprimables} (section~\ref{sec:modele}), suivi d'une validation physique
qui tranche la planéité \emph{réelle} de chaque candidat
(section~\ref{sec:realisation}). Et elle motive l'étude expérimentale
(section~\ref{sec:xp}), qui cherche les contraintes améliorant au mieux le
rendement de planéité.

% =====================================================================
\section{Modélisation}
\label{sec:modele}
% =====================================================================

Nous présentons à présent le modèle PPC qui énumère les substitutions
admissibles. Il comporte une variable par hexagone et un ensemble de contraintes
\emph{structurelles}, toujours actives, que nous décrivons ici. Les contraintes
\emph{additionnelles}, qui font l'objet de l'étude expérimentale, sont
introduites à la section~\ref{sec:xp}.

\subsection{Variables et domaines}

À chaque hexagone $v$ du graphe dual on associe une variable $x_v$ donnant
la taille du cycle substitué :
\[
  x_v \in \{5, 6, 7\}, \qquad v \in V_D.
\]
Toutes les variables ont initialement le même domaine $\{5, 6, 7\}$.
Les contraintes C2, C3 et C4 décrites ci-dessous réduiront ce domaine
en fonction de la position de $v$ dans le graphe dual, allant jusqu'à
le restreindre à $\{6\}$ pour certains hexagones, dits \emph{gelés}.

\subsection{Préalable : motif d'occupation $\sigma(v)$ et blocs libres}
\label{sec:sigma}

Plusieurs des contraintes du modèle reposent sur la même donnée locale :
\emph{où sont placés les voisins de $v$ dans son hexagone}, et
\emph{où sont laissés les côtés libres}. Nous introduisons donc d'abord
le vocabulaire et la notation correspondants, que nous réutiliserons
sans les redéfinir dans la suite.

Soit $v \in V_D$ un hexagone du benzénoïde de départ. Ses six côtés sont
numérotés cycliquement de $0$ à $5$. Chaque côté est, ou bien
\textbf{partagé} avec un voisin (le côté coïncide avec une arête commune
entre $v$ et un autre cycle du benzénoïde, c'est-à-dire une arête de
$\GD$ incidente à $v$), ou bien \textbf{libre} (aucun voisin de ce côté).

\begin{definition}[Motif d'occupation $\sigma(v)$]
Le \emph{motif d'occupation} de $v$ est le vecteur
\[
  \sigma(v) \;=\; (\sigma_0, \sigma_1, \dots, \sigma_5) \;\in\; \{0, 1\}^6 ,
\]
où $\sigma_i = 1$ si le $i$-ème côté de $v$ est partagé avec un voisin,
et $\sigma_i = 0$ s'il est libre. Le nombre de côtés partagés vaut
$\sum_i \sigma_i = \deg_{\GD}(v)$.
\end{definition}

Le motif $\sigma(v)$ est défini à rotation cyclique près : changer
le point de départ de la numérotation produit une rotation de
$\sigma(v)$, mais ne modifie pas l'information géométrique sous-jacente.

\paragraph{Exemple.} Reprenons le benzénoïde $B$ de la
figure~\ref{fig:graphe-carbone}, et concentrons-nous sur le sommet
$v_1$. Dans $\GD(B)$ (figure~\ref{fig:dual}, à droite), $v_1$ a quatre
voisins : $v_0$ à gauche, $v_2$ à droite, $v_3$ en haut-gauche et
$v_4$ en bas-droite. Les quatre côtés de $v_1$ correspondant sont donc
partagés ($\sigma_i = 1$), et les deux côtés restants
(haut-droite et bas-gauche) sont libres ($\sigma_i = 0$). En
choisissant comme point de départ le côté haut-droite, on obtient
\[
  \boxed{\;\sigma(v_1) \;=\; (0, 1, 1, 0, 1, 1) \;,\;}
\]
ce qui est équivalent, à une rotation près, à $(1, 1, 0, 1, 1, 0)$.
La figure~\ref{fig:sigma-v1} matérialise visuellement cette
décomposition.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.3, baseline=(current bounding box.center)]
  \def\rhex{1.4}
  % Sommets de l'hexagone (pointy-top, sommets aux angles 30 + 60k)
  \foreach \i in {0,...,5} {
    \coordinate (S\i) at ({30 + 60*\i}:\rhex);
  }
  % Numerotation des cotes :
  %   cote i = entre sommet i et sommet i+1 (mod 6)
  %   - cote 0 : entre S0 (30°) et S1 (90°), milieu a 60° (haut-droite)
  %   - cote 1 : entre S1 (90°) et S2 (150°), milieu a 120° (haut-gauche)
  %   - cote 2 : entre S2 (150°) et S3 (210°), milieu a 180° (gauche)
  %   - cote 3 : entre S3 (210°) et S4 (270°), milieu a 240° (bas-gauche)
  %   - cote 4 : entre S4 (270°) et S5 (330°), milieu a 300° (bas-droite)
  %   - cote 5 : entre S5 (330°) et S0 (30°), milieu a 0° (droite)
  % sigma(v1) = (0, 1, 1, 0, 1, 1) en partant du cote haut-droite (60°)
  % Cotes partages (sigma_i = 1) en bleu epais, cotes libres (sigma_i = 0)
  % en rouge pointille epais.
  \tikzset{shared/.style={ultra thick, blue!70!black},
           free/.style={ultra thick, red!70!black, densely dashed}}
  \draw[free]    (S0)--(S1);  % cote 0 : libre (haut-droite)
  \draw[shared]  (S1)--(S2);  % cote 1 : partage avec v3 (haut-gauche)
  \draw[shared]  (S2)--(S3);  % cote 2 : partage avec v0 (gauche)
  \draw[free]    (S3)--(S4);  % cote 3 : libre (bas-gauche)
  \draw[shared]  (S4)--(S5);  % cote 4 : partage avec v4 (bas-droite)
  \draw[shared]  (S5)--(S0);  % cote 5 : partage avec v2 (droite)
  % Etiquette centrale
  \node at (0,0) {\large $v_1$};
  % Annotations des cotes (sigma_i)
  \foreach \i/\ang in {0/60, 1/120, 2/180, 3/240, 4/300, 5/0} {
    \pgfmathsetmacro{\rext}{1.30}
    \node[font=\small] at ({\ang}:{\rhex*\rext}) {$\sigma_\i$};
  }
  % Etiquettes voisins, places a l'exterieur des cotes partages
  \pgfmathsetmacro{\rvois}{2.05}
  \node[font=\small, gray] at ({120}:\rvois) {$v_3$};
  \node[font=\small, gray] at ({180}:\rvois) {$v_0$};
  \node[font=\small, gray] at ({300}:\rvois) {$v_4$};
  \node[font=\small, gray] at ({0}:\rvois)   {$v_2$};
\end{tikzpicture}
\hspace{0.8cm}
\begin{minipage}[c]{0.32\linewidth}
\centering
\small
\textbf{Motif d'occupation}\\[0.3em]
$\sigma(v_1) = (0, 1, 1, 0, 1, 1)$\\[0.6em]
\begin{tabular}{cl}
\textcolor{blue!70!black}{\rule{1em}{1pt}} & côté \emph{partagé} ($\sigma_i = 1$) \\[0.2em]
\textcolor{red!70!black}{\rule{1em}{1pt}\hspace{-1em}\rule{1em}{1pt}} & côté \emph{libre} ($\sigma_i = 0$) \\
\end{tabular}\\[0.5em]
$b(v_1) = 2$ blocs libres\\[0.2em]
(deux zéros isolés)
\end{minipage}
\caption{Motif d'occupation $\sigma(v_1)$ pour le sommet central du
benzénoïde de la figure~\ref{fig:graphe-carbone}. Les côtés partagés
avec un voisin (en bleu plein) alternent avec les côtés libres (en
rouge pointillé). Comme les deux côtés libres sont \emph{isolés}
(non consécutifs cycliquement), $v_1$ ne possède pas de paire de côtés
libres adjacents : c'est exactement la condition qui empêchera $v_1$
de se contracter en pentagone (contrainte C2 ci-dessous).}
\label{fig:sigma-v1}
\end{figure}

\begin{definition}[Blocs libres, $b(v)$]
On appelle \emph{bloc libre} de $v$ une suite maximale d'indices
$i, i+1, \dots, j-1$ (modulo $6$) telle que
$\sigma_i = \sigma_{i+1} = \dots = \sigma_{j-1} = 0$, encadrée par
des côtés partagés. On note $b(v)$ le nombre de blocs libres
distincts de $v$.
\end{definition}

Pour l'exemple de la figure~\ref{fig:sigma-v1}, les côtés libres sont
en positions $0$ et $3$, séparés par des côtés partagés. Chacun
constitue son propre bloc, et $b(v_1) = 2$.

\begin{definition}[Sommet intérieur libre]
Un \emph{sommet intérieur libre} de $v$ est un sommet du cycle dont les
\emph{deux} côtés adjacents sont libres. De manière équivalente, $v$
possède un sommet intérieur libre si et seulement si $\sigma(v)$
contient au moins deux zéros consécutifs cycliquement.
\end{definition}

Cette notion va jouer un rôle clef dans la contrainte C2 :
contracter un hexagone en pentagone consiste précisément à
\emph{supprimer un sommet intérieur libre}. Pour
$v_1$ de la figure~\ref{fig:sigma-v1}, chaque côté libre est isolé
entre deux côtés partagés, donc aucun sommet du cycle n'a ses deux
côtés adjacents libres simultanément : $v_1$ \emph{n'a pas} de sommet
intérieur libre.

\subsection{Contraintes}
\label{sec:contraintes}

Cette sous-section regroupe les contraintes du modèle. C1, C2, C3, C5
et C6 sont actives par défaut. C4 et C7 sont optionnelles : leur effet
est étudié expérimentalement à la section~\ref{sec:xp}.

\subsubsection{C1 --- Conservation du carbone}
\label{sec:c1}

Le nombre total d'atomes de carbone doit être préservé par la
substitution. Notons $n_H = n_H(B)$ le nombre d'hexagones du benzénoïde $B$
de départ, et $n_5$, $n_6$, $n_7$ les nombres respectifs de
pentagones, hexagones et heptagones dans la solution. Tout cycle de la
solution remplace exactement un hexagone du benzénoïde de départ, donc
\[
  n_5 + n_6 + n_7 \;=\; n_H .
\]

Pour exprimer la conservation du carbone, comptons explicitement les
atomes de carbone de chaque côté. Soit $a = |E_D|$ le nombre d'arêtes
du graphe dual $\GD$ du benzénoïde de départ (chaque arête correspond
à deux hexagones partageant une arête carbone--carbone, et donc deux
atomes communs). Le nombre total d'atomes de carbone du benzénoïde de
départ vaut
\[
  N(B) \;=\; 6 n_H - 2 a ,
\]
puisque chaque hexagone contient $6$ atomes et que chaque arête
commune correspond à $2$ atomes comptés en double.

Après substitution, la solution comporte $n_5$ pentagones, $n_6$
hexagones et $n_7$ heptagones, soit $5 n_5 + 6 n_6 + 7 n_7$ atomes
avant correction. La \textbf{conservation du graphe dual}
(section~\ref{sec:probleme}) garantit que \textbf{le nombre d'arêtes
communes reste} $a$, donc le nombre total d'atomes de la solution vaut
\[
  N(\text{solution}) \;=\; 5 n_5 + 6 n_6 + 7 n_7 - 2 a .
\]

Conserver le nombre d'atomes ($N(\text{solution}) = N(B)$) donne donc
\[
  5 n_5 + 6 n_6 + 7 n_7 - 2 a \;=\; 6 n_H - 2 a ,
\]
et le terme $-2a$ se simplifie de part et d'autre. On obtient
\[
  5 n_5 + 6 n_6 + 7 n_7 \;=\; 6 n_H .
\]
Cette égalité porte sur les comptes $n_5, n_6, n_7$, qui ne sont pas
des variables du modèle. Pour l'exprimer comme une contrainte sur les
variables $x_v$, on remarque que la somme $\sum_{v \in V_D} x_v$ vaut
exactement $5 n_5 + 6 n_6 + 7 n_7$, puisque chaque hexagone $v$
contribue sa taille $x_v$ à la somme selon le nombre de fois qu'elle
apparaît. C'est précisément à cette étape que l'on passe d'une
identité mathématique sur des comptages à une contrainte exploitable
par le solveur :
\[
  \boxed{\; \sum_{v \in V_D} x_v \;=\; 5 n_5 + 6 n_6 + 7 n_7 \;=\; 6 n_H \;,}
\]
qui est l'expression effectivement déclarée au solveur : une simple
contrainte de somme sur les variables entières.

Combinée à $n_5 + n_6 + n_7 = n_H$, cette équation équivaut à
$n_5 = n_7$, qui s'interprète chimiquement : chaque pentagone retire
un atome de carbone et doit être compensé par un heptagone qui en
ajoute un.

\subsubsection{C2 --- Restriction géométrique du domaine}
\label{sec:c2}

L'attribution $x_v \in \{5, 6, 7\}$ doit être compatible avec une
\emph{reconstruction géométrique} de $v$ qui ne brise pas la fusion
avec ses voisins (les côtés partagés doivent rester partagés, à la même
position). Cette compatibilité dépend uniquement du motif d'occupation
$\sigma(v)$ et peut donc être vérifiée \emph{en amont} de la résolution,
sous forme d'une restriction du domaine de $x_v$.

\paragraph{Contraction en pentagone ($x_v = 5$).}
Transformer un hexagone en pentagone revient à supprimer l'un de ses
sommets. Pour ne pas casser un côté partagé, ce sommet doit avoir ses
deux côtés adjacents libres : c'est précisément un \emph{sommet
intérieur libre}, au sens de la définition donnée
section~\ref{sec:sigma}. La valeur $x_v = 5$ est donc \textbf{admissible}
si et seulement si $\sigma(v)$ contient au moins deux zéros consécutifs
cycliquement.

\paragraph{Dilatation en heptagone ($x_v = 7$).}
Transformer un hexagone en heptagone revient à ajouter un sommet entre
deux sommets existants, c'est-à-dire à dédoubler un côté libre. La
valeur $x_v = 7$ est donc admissible si et seulement si $\sigma(v)$
contient au moins un côté libre, soit $\sigma_i = 0$ pour au moins
un $i$.

\paragraph{Énoncé formel.}
Pour chaque sommet $v \in V_D$, la contrainte C2 réduit le domaine
$D(x_v)$ à
\[
  D(x_v) \;=\; \{6\}
              \;\cup\; \bigl\{5 \,:\, \sigma(v) \text{ a } \ge 2 \text{ zéros consécutifs}\bigr\}
              \;\cup\; \bigl\{7 \,:\, \sigma(v) \text{ a } \ge 1 \text{ zéro}\bigr\} ,
\]
où la valeur $6$ reste toujours admissible (l'hexagone d'origine est, par
construction, géométriquement compatible avec lui-même).

C2 s'implémente comme un prétraitement~: pour chaque $v$, on calcule
$\sigma(v)$ depuis $\GD$ et on initialise $D(x_v)$ en conséquence avant
de passer le modèle au solveur. La présence ou l'absence d'une valeur
dans $D(x_v)$ joue alors exactement le rôle d'une contrainte unaire
sur $x_v$.

\paragraph{Exemple.} Reprenons le sommet $v_1$ de la
figure~\ref{fig:sigma-v1}, dont le motif est
$\sigma(v_1) = (0, 1, 1, 0, 1, 1)$. Les zéros sont en positions $0$
et $3$, séparés par des côtés partagés : il n'y a aucune paire de zéros
consécutifs cycliquement. La contrainte C2 retire donc $5$ du domaine
de $x_{v_1}$, mais conserve $7$ (le motif contient bien un zéro). On
obtient
\[
  D(x_{v_1}) \;=\; \{6, 7\} .
\]
Sur cet exemple, $v_1$ ne pourra pas être contracté en pentagone, mais
pourra encore être dilaté en heptagone (en insérant un sommet sur le
côté libre $\sigma_0$ ou $\sigma_3$).

\subsubsection{C3 --- Gel topologique des hexagones de degré $6$}
\label{sec:c3-gel-deg6}

C3 est en fait un cas particulier de C2, mais suffisamment important
pour mériter d'être isolé : il correspond à la situation où la
restriction de domaine se réduit à $\{6\}$ pour des raisons purement
topologiques.

Si $\deg_{\GD}(v) = 6$, l'hexagone $v$ est entouré de six voisins et
n'a aucun côté libre. Son motif vaut $\sigma(v) = (1, 1, 1, 1, 1, 1)$ :
il ne contient aucun zéro, donc ni 5 ni 7 ne sont admissibles par C2.
On a alors directement
\[
  D(x_v) \;=\; \{6\} \quad\Longleftrightarrow\quad x_v \;=\; 6 .
\]
Le sommet $v$ est dit \emph{gelé}, et il n'apparaît plus que comme
constante dans le modèle. Ce gel est une conséquence topologique
stricte, sans alternative possible.

\subsubsection{C4 --- Gel des hexagones à blocs libres fragmentés (optionnel)}
\label{sec:c4-fragmentes}

Si $b(v) \ge 2$, les côtés libres de $v$ sont répartis en plusieurs blocs disjoints sur le cycle.
Notons que la simple connaissance du \emph{nombre} d'arêtes périphériques
d'un hexagone --- au sens de l'étiquetage de Brinkmann repris par
Varet~\cite{varet2022} pour distinguer les benzénoïdes partageant le
même graphe dual intérieur --- ne suffit pas à caractériser cette
situation : c'est la \emph{disposition} des côtés libres (un bloc
continu ou plusieurs blocs séparés) qui détermine la faisabilité d'une
substitution.

La transformation n'est pas \emph{impossible}, mais elle n'est plus
unique : on doit choisir où placer l'arête périphérique supplémentaire
introduite par le septième sommet, et chaque choix produit un benzénoïde
distinct au sens de Brinkmann. La figure~\ref{fig:c2-2-illustration}
illustre cet enjeu sur un motif tiré de la figure~2.14 de la
thèse de Varet~\cite{varet2022} : un anthracène dont l'hexagone central
est étiqueté $2{+}2$ (deux blocs périphériques de deux arêtes, en haut
et en bas) admet deux substitutions distinctes par un heptagone, l'une
ajoutant une arête périphérique en bas (étiquetage $2{+}3$), l'autre en
haut ($3{+}2$).

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=0.6, every node/.style={font=\small}]
  \tikzset{
    polyL/.style={thick, green!50!black},
    polyR/.style={thick, blue!70!black},
    polyC/.style={thick, red!80!black},
    labL/.style={green!50!black, font=\normalsize},
    labR/.style={blue!70!black, font=\normalsize},
    labC/.style={red!80!black, font=\normalsize},
    arr/.style={-{Stealth[length=3mm]}, thick, gray!40!black},
    subcap/.style={font=\normalsize}
  }
  \def\rsix{1.4}
  \def\dhex{2.4249}    % distance centre-centre hex-hex (= rsix * sqrt(3))

  % ============================================================
  % (a) ANTHRACENE EN HAUT : 3 hexagones, etiquetage 5, 2+2, 5
  % ============================================================
  \begin{scope}[shift={(0, 4.5)}]
    % Hex central a (0,0), sommets a 30 + k*60
    \foreach \i in {0,...,5} {
      \coordinate (C\i) at ({30 + \i*60}:\rsix);
    }
    % Hex gauche a (-dhex, 0)
    \begin{scope}[shift={(-\dhex, 0)}]
      \foreach \i in {0,...,5} { \coordinate (L\i) at ({30 + \i*60}:\rsix); }
      \draw[polyL] (L0)--(L1)--(L2)--(L3)--(L4)--(L5)--cycle;
    \end{scope}
    % Hex droit a (dhex, 0)
    \begin{scope}[shift={(\dhex, 0)}]
      \foreach \i in {0,...,5} { \coordinate (R\i) at ({30 + \i*60}:\rsix); }
      \draw[polyR] (R0)--(R1)--(R2)--(R3)--(R4)--(R5)--cycle;
    \end{scope}
    % Hex central
    \draw[polyC] (C0)--(C1)--(C2)--(C3)--(C4)--(C5)--cycle;
    % Etiquettes
    \node[labL] at (-4.0, 0) {$5$};
    \node[labR] at (4.0, 0) {$5$};
    \node[labC] at (0, 1.85) {$2$};
    \node[labC] at (0, -1.85) {$2$};
    \node[subcap] at (0, -2.7) {(a) anthracène};
  \end{scope}

  % ============================================================
  % (b) VARIANTE 1 EN BAS GAUCHE : heptagone REGULIER central, etiquete 2+3
  % ============================================================
  % Geometrie : heptagone regulier de rayon r7 = 1.4/(2 sin(pi/7)) ~ 1.6132,
  % apex haut a (0, r7). Les 2 hex voisins partagent les aretes V1-V2
  % (cote gauche, oblique) et V5-V6 (cote droit, oblique). Du coup leurs
  % centres ne sont plus alignes a y=0 : ils sont surrieleves a y=+0.593
  % et orientes de telle sorte qu'un sommet tombe a angle ~17.15°.
  \begin{scope}[shift={(-5.5, -3.8)}]
    \pgfmathsetmacro{\rsept}{1.4/(2*sin(180/7))}
    \foreach \i in {0,...,6} {
      \coordinate (V\i) at ({90 + \i*360/7}:\rsept);
    }
    % Hex gauche : centre decale et tourne pour partager l'arete V1-V2
    \begin{scope}[shift={(-2.599, 0.593)}]
      \foreach \i in {0,...,5} {
        \coordinate (L\i) at ({-42.85 + \i*60}:\rsix);
      }
      \draw[polyL] (L0)--(L1)--(L2)--(L3)--(L4)--(L5)--cycle;
    \end{scope}
    % Hex droit : symetrique (partage l'arete V5-V6)
    \begin{scope}[shift={(2.599, 0.593)}]
      \foreach \i in {0,...,5} {
        \coordinate (R\i) at ({162.85 + \i*60}:\rsix);
      }
      \draw[polyR] (R0)--(R1)--(R2)--(R3)--(R4)--(R5)--cycle;
    \end{scope}
    \draw[polyC] (V0)--(V1)--(V2)--(V3)--(V4)--(V5)--(V6)--cycle;
    \node[labL] at (-4.3, 0.593) {$5$};
    \node[labR] at (4.3, 0.593) {$5$};
    \node[labC] at (0, 2.05) {$2$};
    \node[labC] at (0, -1.85) {$3$};
    \node[subcap] at (0, -2.7) {(b) variante 1 : $2{+}3$};
  \end{scope}

  % ============================================================
  % (c) VARIANTE 2 EN BAS DROITE : heptagone REGULIER central, etiquete 3+2
  % ============================================================
  % Miroir vertical de la variante 1 : apex en bas, hex voisins abaisses.
  \begin{scope}[shift={(5.5, -3.8)}]
    \pgfmathsetmacro{\rsept}{1.4/(2*sin(180/7))}
    \foreach \i in {0,...,6} {
      \coordinate (W\i) at ({-90 + \i*360/7}:\rsept);
    }
    % Hex gauche : partage l'arete W5-W6
    \begin{scope}[shift={(-2.599, -0.593)}]
      \foreach \i in {0,...,5} {
        \coordinate (L\i) at ({42.85 + \i*60}:\rsix);
      }
      \draw[polyL] (L0)--(L1)--(L2)--(L3)--(L4)--(L5)--cycle;
    \end{scope}
    % Hex droit : partage l'arete W1-W2
    \begin{scope}[shift={(2.599, -0.593)}]
      \foreach \i in {0,...,5} {
        \coordinate (R\i) at ({137.15 + \i*60}:\rsix);
      }
      \draw[polyR] (R0)--(R1)--(R2)--(R3)--(R4)--(R5)--cycle;
    \end{scope}
    \draw[polyC] (W0)--(W1)--(W2)--(W3)--(W4)--(W5)--(W6)--cycle;
    \node[labL] at (-4.3, -0.593) {$5$};
    \node[labR] at (4.3, -0.593) {$5$};
    \node[labC] at (0, 1.85) {$3$};
    \node[labC] at (0, -2.05) {$2$};
    \node[subcap] at (0, -2.7) {(c) variante 2 : $3{+}2$};
  \end{scope}

  % ============================================================
  % Fleches obliques de l'anthracene vers chaque variante
  % ============================================================
  \draw[arr] (-1.8, 2.2) -- (-4.2, -1.4);
  \draw[arr] (1.8, 2.2)  -- (4.2, -1.4);

\end{tikzpicture}
\caption{Anthracène et transformation du cycle central en heptagone.}
\label{fig:c2-2-illustration}
\end{figure}

On impose donc $x_v = 6$ \emph{par défaut} pour ces hexagones, ce qui
revient à interdire l'ambiguïté de transformation introduite par les
blocs libres multiples. Cette restriction est désactivable lorsqu'on
souhaite explorer des géométries où l'on accepte de modifier
l'étiquetage des voisins, par exemple pour étudier l'effet de relâcher
cette contrainte dans les expériences de la section~\ref{sec:xp}.

Les hexagones qui ne sont gelés par aucune des deux règles sont dits
\emph{libres} et conservent le domaine $\{5, 6, 7\}$.

\subsubsection{C5 --- Voisinage admissible et table de voisinage}
\label{sec:c5-table}

C'est le cœur du filtrage chimique. Pour chaque hexagone libre $v$, on impose
que la configuration locale formée par $v$ et ses voisins soit
\emph{réalisable}. Cette condition est encodée par une \textbf{contrainte
en extension} : le tuple ordonné
\[
  \tau_v = (x_v, x_{u_1}, x_{u_2}, \dots, x_{u_k}),
\]
où $u_1, \dots, u_k$ sont les voisins de $v$ dans \GD{} pris dans
l'ordre cyclique, doit appartenir à une \textbf{table de voisinage}
\Tn{} pré-calculée. Le degré $k$ de $v$ dans le graphe dual est
compris entre $2$ et $6$ (un hexagone a au plus six côtés partagés),
ce qui donne à $\tau_v$ une longueur $|\tau_v| = k + 1 \in
\{3, 4, 5, 6, 7\}$. Le cas $k = 0$ (un hexagone isolé sans voisin)
n'est pas considéré, et le cas $k = 1$ est traité séparément
ci-dessous.

Un point essentiel distingue notre table de celle des benzénoïdes purs :
elle est \emph{sensible à la disposition} des voisins. Deux configurations
ayant les mêmes tailles de voisins mais disposées différemment autour du
cycle central ne sont pas équivalentes, car elles ne se reconstruisent pas
de la même manière en trois dimensions. La position des voisins est donc
encodée explicitement dans les tuples de la table (un $0$ marquant un côté
libre).

La table est \textbf{construite hors-ligne} : on énumère les configurations
locales candidates (un cycle central entouré de voisins de tailles données
dans une disposition donnée), on reconstruit chacune en 3D, on l'optimise
par \xtb{}, et on ne retient que celles qui sont planes (au sens du critère
décrit en section~\ref{sec:realisation}). Ce catalogue est
\emph{paramétrable} : il pourra être régénéré ou affiné lorsque de nouvelles
études chimiques préciseront les critères d'admissibilité. La version
utilisée dans ce travail comporte \textbf{721} entrées au total,
réparties selon la taille du cycle central :

\begin{center}
\small
\begin{tabular}{lccc}
\toprule
 & $\mathcal{T}(5)$ & $\mathcal{T}(6)$ & $\mathcal{T}(7)$ \\
\midrule
Nombre d'entrées & $63$ & $291$ & $367$ \\
\bottomrule
\end{tabular}
\end{center}

\begin{remarque}
Un hexagone de degré $1$ (un seul voisin) n'a pas besoin de contrainte de
table : un cycle accolé à un unique voisin par une arête est toujours
réalisable, quelle que soit sa taille.
\end{remarque}

\subsubsection{C6 --- Rupture de symétrie}
\label{sec:c6}

Deux substitutions qui ne diffèrent que par une symétrie du benzénoïde
décrivent la \emph{même} molécule. Pour ne les compter qu'une fois, on
exploite le groupe des automorphismes du graphe dual, $\Aut(\GD)$ (les
permutations des sommets qui préservent les arêtes), dont on calcule un
ensemble générateur $\{\pi_1, \dots, \pi_m\}$. Pour chaque générateur $\pi$,
on impose une contrainte \emph{lex-leader}
\[
  (x_0, x_1, \dots, x_{h-1}) \;\le_{\text{lex}}\; (x_{\pi(0)}, x_{\pi(1)}, \dots, x_{\pi(h-1)}),
\]
qui ne retient, dans chaque orbite de solutions équivalentes, que le
représentant minimal pour l'ordre lexicographique. Cette technique élimine
les doublons et réduit fortement l'espace de recherche. Sa validité repose
sur l'unicité du plongement planaire des graphes considérés (théorème de
Whitney~\cite{whitney} et 2-connexité des benzénoïdes).

\subsubsection{C7 --- Adjacence $5$--$7$ (motif azulène)}
\label{sec:c7}

Cette contrainte est une piste d'amélioration explorée à la
section~\ref{sec:xp} ; elle n'est pas active par défaut. L'idée chimique
sous-jacente est la suivante : remplacer un motif $6$--$6$ (deux hexagones
accolés, comme dans le naphtalène) par un motif $5$--$7$ (un pentagone et
un heptagone accolés, comme dans l'azulène, voir
figure~\ref{fig:adj-5-7}) \emph{préserve} le compte d'atomes de carbone
($5 + 7 = 6 + 6 = 12$, moins $2$ pour l'arête partagée, donc $10$ dans les
deux cas) et conserve le nombre total d'électrons $\pi$ ($10$ électrons,
soit $4n+2$ avec $n=2$, dans les deux motifs : la règle de Hückel reste
satisfaite). Le motif $5$--$7$ apparaît donc comme un \emph{substitut
isoélectronique} naturel du motif $6$--$6$, et il est connu en chimie pour
préserver la planéité localement, là où un pentagone ou un heptagone isolé
introduit une courbure.

\begin{figure}[h]
\centering
\begin{tikzpicture}[scale=1.0]
  % Pentagone regulier (cf. fig.~\ref{fig:azulene})
  \pgfmathsetmacro{\rP}{1.4/(2*sin(36))}
  \pgfmathsetmacro{\hP}{\rP*cos(36)}
  \coordinate (CP) at (-\hP, 0);
  \coordinate (P0) at ($(CP)+({\rP*cos(36)},{\rP*sin(36)})$);
  \coordinate (P1) at ($(CP)+({\rP*cos(108)},{\rP*sin(108)})$);
  \coordinate (P2) at ($(CP)+({\rP*cos(180)},{\rP*sin(180)})$);
  \coordinate (P3) at ($(CP)+({\rP*cos(252)},{\rP*sin(252)})$);
  \coordinate (P4) at ($(CP)+({\rP*cos(324)},{\rP*sin(324)})$);
  \draw (P0)--(P1)--(P2)--(P3)--(P4)--cycle;
  \node at (CP) {$5$};
  % Heptagone regulier
  \pgfmathsetmacro{\rH}{1.4/(2*sin(180/7))}
  \pgfmathsetmacro{\hH}{\rH*cos(180/7)}
  \pgfmathsetmacro{\angA}{180-(180/7)}
  \coordinate (CH) at (\hH, 0);
  \coordinate (H0) at ($(CH)+({\rH*cos(\angA)},{\rH*sin(\angA)})$);
  \coordinate (H1) at ($(CH)+({\rH*cos(\angA-360/7)},{\rH*sin(\angA-360/7)})$);
  \coordinate (H2) at ($(CH)+({\rH*cos(\angA-2*360/7)},{\rH*sin(\angA-2*360/7)})$);
  \coordinate (H3) at ($(CH)+({\rH*cos(\angA-3*360/7)},{\rH*sin(\angA-3*360/7)})$);
  \coordinate (H4) at ($(CH)+({\rH*cos(\angA-4*360/7)},{\rH*sin(\angA-4*360/7)})$);
  \coordinate (H5) at ($(CH)+({\rH*cos(\angA-5*360/7)},{\rH*sin(\angA-5*360/7)})$);
  \coordinate (H6) at ($(CH)+({\rH*cos(\angA-6*360/7)},{\rH*sin(\angA-6*360/7)})$);
  \draw (H0)--(H1)--(H2)--(H3)--(H4)--(H5)--(H6);
  \node at (CH) {$7$};
\end{tikzpicture}
\caption{Motif $5$--$7$ fusionné (azulène).}
\label{fig:adj-5-7}
\end{figure}

Pour chaque sommet $v$ du graphe dual, notons
\[
  \mathcal{N}(v) \;=\; \{\, u \in V_D \mid (u, v) \in E_D \,\}
\]
l'ensemble des voisins de $v$ dans \GD{}. La contrainte C7 traduit
alors l'idée chimique dans le modèle PPC sous la forme
\emph{tout pentagone a au moins un heptagone parmi ses voisins, et tout
heptagone a au moins un pentagone parmi les siens} :
\[
  \forall v \in V_D,\quad
  \bigl( x_v = 5 \Rightarrow \exists u \in \mathcal{N}(v),\ x_u = 7 \bigr)
  \;\wedge\;
  \bigl( x_v = 7 \Rightarrow \exists u \in \mathcal{N}(v),\ x_u = 5 \bigr).
\]
Cette contrainte est purement \emph{locale}, mais sa motivation est globale :
en imposant que tout défaut $5$ ou $7$ soit immédiatement compensé par un
défaut opposé, on espère préserver la planéité d'ensemble. Son efficacité
réelle est mesurée expérimentalement.

\subsection{Résolution}

Le modèle est écrit en \textsc{PyCSP3}, puis compilé au format standard
XCSP3. Selon les options choisies par l'utilisateur, il inclut les
contraintes suivantes :
\begin{itemize}
  \item \textbf{actives par défaut :} C1 (conservation du carbone), C2
    (restriction géométrique du domaine), C3 (gel des hexagones de
    degré $6$), C5 (voisinage admissible) et C6 (rupture de symétrie) ;
  \item \textbf{optionnelles :} C4 (gel des hexagones à blocs libres
    fragmentés, activée par défaut mais désactivable) et C7 (adjacence
    $5$--$7$, désactivée par défaut, étudiée à la section~\ref{sec:xp}).
\end{itemize}

Le solveur \ace{} est ensuite invoqué pour \textbf{énumérer toutes les
solutions} de l'instance ainsi configurée. Chaque solution est un vecteur
de tailles $(x_0, \dots, x_{h-1})$ qui sera, à l'étape suivante,
reconstruit en une molécule tridimensionnelle puis validé.

% =====================================================================
\section{Réalisation : des solutions du modèle aux molécules validées et analysées}
\label{sec:realisation}

Le modèle de la section~\ref{sec:modele} produit des \emph{vecteurs de tailles}
$(x_0, \dots, x_{h-1})$ : des objets abstraits. Cette section décrit comment on
en fait des molécules réelles, comment on décide de leur planéité, et comment on
analyse leurs propriétés électroniques. Elle regroupe l'essentiel du travail
d'implémentation.

\subsection{Reconstruction 3D, validation et test de planéité}
\label{sec:reconstruction-validation}

Cette sous-section décrit, en trois temps, comment on passe d'un vecteur
de tailles abstrait à une molécule physiquement validée : la
reconstruction 3D produit une géométrie initiale, la validation \xtb{}
la fait converger vers son équilibre énergétique, et un test géométrique
sur la géométrie optimisée tranche la planéité.

\subsubsection{Reconstruction 3D des solutions}
\label{sec:reconstruction}

Chaque solution du modèle est un vecteur de tailles $(x_0, \dots, x_{h-1})$
indiquant la taille assignée à chaque hexagone. Pour en faire une molécule
tridimensionnelle exploitable, on procède en trois phases : modification
topologique du graphe carbone, placement géométrique des sommets, puis
assemblage en molécule (liaisons doubles et hydrogènes).

\paragraph{Modifications topologiques du graphe carbone.}

En partant du graphe carbone (défini en section~\ref{sec:dual}) du
benzénoïde d'entrée, lorsque la solution affecte une taille $x_v \neq 6$
à un hexagone $v$, il faut transformer ce cycle en un pentagone ou un
heptagone \emph{sans} perturber les arêtes partagées avec ses voisins.

\paragraph{Repérage des côtés libres.}
Rappelons que le motif d'occupation $\sigma(v) \in \{0,1\}^6$ indique pour
chacun des six côtés du cycle s'il est partagé avec un voisin ($\sigma_i = 1$)
ou \emph{libre} ($\sigma_i = 0$). Les modifications topologiques opèrent
exclusivement sur les sommets adjacents à des côtés libres ; les sommets
attenants à au moins un côté partagé sont \emph{gelés}, sans quoi la
modification briserait la fusion avec un voisin.

\paragraph{Contraction en pentagone ($x_v = 5$).}
On supprime un sommet du cycle parmi les sommets libres, en privilégiant celui
qui se trouve au milieu d'un bloc de côtés libres consécutifs ; ses deux
voisins dans le cycle sont alors reliés directement, réduisant le cycle de six
à cinq sommets. Le choix du sommet à supprimer est unique lorsque l'hexagone
possède un seul bloc libre (cas garanti par la condition $b(v) \le 1$ pour les
hexagones non gelés, section~\ref{sec:modele}).

\paragraph{Dilatation en heptagone ($x_v = 7$).}
On insère un nouveau sommet entre deux sommets libres consécutifs (au milieu
du bloc libre), ce qui ajoute une arête au cycle et le porte à sept sommets.

\paragraph{Effet sur les voisins.}
Une fois le cycle modifié, sa nouvelle séquence ordonnée de sommets est
conservée dans la structure de données du graphe carbone afin que les cycles
voisins continuent à le référencer correctement via leurs sommets partagés.

\paragraph{Placement géométrique.}
\label{sec:placement}

Une fois la topologie fixée (chaque cycle connaît sa liste ordonnée de
sommets), il reste à attribuer à chaque sommet une position dans le plan.
L'idée est de poser chaque cycle comme un \emph{polygone régulier} de longueur
de côté $\ell = 1{,}4$~\AA{} (longueur typique d'une liaison
carbone--carbone), en respectant les contraintes de partage d'arête entre
cycles adjacents.

\paragraph{Rayon des cycles.}
Le rayon $r_n$ du cercle circonscrit à un polygone régulier de $n$ côtés de
longueur $\ell$ vaut
\[
  r_n \;=\; \frac{\ell}{2\sin(\pi/n)}.
\]
Pour $\ell = 1{,}4$~\AA{} (longueur typique d'une liaison carbone--carbone
dans un benzénoïde), on obtient $r_5 \approx 1{,}19$~\AA, $r_6 \approx
1{,}40$~\AA, $r_7 \approx 1{,}61$~\AA.

\paragraph{Parcours en largeur du graphe dual.}
Le placement s'effectue par un parcours en largeur (\emph{BFS}) du graphe
dual \GD{} à partir d'un cycle \textbf{ancre} --- choisi de plus haut degré,
ce qui maximise les contraintes immédiates de partage et stabilise la
géométrie. Le cycle ancre est dessiné centré à l'origine, dans une orientation
canonique. Chaque cycle suivant est placé \emph{relativement à un parent déjà
placé}, via l'arête $e = \{a, b\}$ qu'ils partagent.

\paragraph{Positionnement d'un cycle enfant.}
Soit $c$ un cycle à placer, parent $p$, arête partagée $\{a, b\}$ déjà
positionnée aux coordonnées $\mathbf{x}_a, \mathbf{x}_b$. Le placement
s'effectue alors en quatre étapes décrites par
l'algorithme~\ref{algo:place-enfant}.

\begin{algorithm}[h]
\small\ttfamily
\textbf{Entrée :} cycle c de taille n\_c, parent p déjà placé, arête \{a, b\} partagée\\
\textbf{Sortie :} positions des sommets de c dans le plan\\[0.3em]
m $\leftarrow$ (x\_a + x\_b) / 2\hfill {\rm ; milieu de l'arête partagée}\\
$a$\_\{n\_c\} $\leftarrow$ $r$\_\{n\_c\} $\cdot$ cos($\pi$ / n\_c)\hfill {\rm ; apothème du polygone régulier}\\
n $\leftarrow$ vecteur unitaire $\perp$ \{a, b\}, opposé au parent p\\
centre\_c $\leftarrow$ m + $a$\_\{n\_c\} $\cdot$ n\\[0.3em]
{\rm // répartir les n\_c$-$2 sommets restants sur le cercle}\\
pour i de 1 à n\_c $-$ 2 :\\
\phantom{xxxx} $\theta_i$ $\leftarrow$ angle du i-ème sommet (depuis a vers b, hors parent)\\
\phantom{xxxx} x\_i $\leftarrow$ centre\_c + r\_\{n\_c\} $\cdot$ (cos $\theta_i$, sin $\theta_i$)
\caption{Positionnement d'un cycle enfant relativement à son parent.}
\label{algo:place-enfant}
\end{algorithm}

Le choix de l'orientation de la normale par rapport au parent (étape 3
de l'algorithme) garantit que la structure ne se replie pas sur
elle-même : chaque cycle enfant est posé du \emph{bon côté} de son arête
de fusion. Lorsqu'un cycle a plusieurs parents déjà placés (cas de
cycles internes au benzénoïde), seul le premier rencontré dans le BFS
est utilisé pour le placer, et les positions imposées par les autres
parents sont vérifiées \emph{a posteriori} pour cohérence ; la topologie
en amont garantit qu'elles coïncident dans la limite des polygones
réguliers.

\paragraph{Algorithme global.}
L'algorithme complet de placement, qui orchestre le parcours en
largeur du graphe dual et appelle l'algorithme~\ref{algo:place-enfant}
sur chaque cycle non encore placé, est donné par
l'algorithme~\ref{algo:place-bfs}.

\begin{algorithm}[h]
\small\ttfamily
placer le cycle ancre à l'origine, orienté canoniquement\\
file $\leftarrow$ [ancre]\\
tant que file non vide :\\
\phantom{xxxx} c $\leftarrow$ file.popleft()\\
\phantom{xxxx} pour chaque voisin v de c dans GD, non encore placé :\\
\phantom{xxxxxxxx} \{a, b\} $\leftarrow$ arête partagée entre c et v\\
\phantom{xxxxxxxx} positionner v par l'algorithme~\ref{algo:place-enfant} (parent = c)\\
\phantom{xxxxxxxx} file.append(v)
\caption{Placement géométrique global par parcours en largeur du graphe dual.}
\label{algo:place-bfs}
\end{algorithm}

\paragraph{Assemblage moléculaire.}
\label{sec:assemblage}

Une fois tous les sommets placés en 2D (avec $z=0$), il reste à transformer la
géométrie en une véritable molécule. On effectue trois opérations successives.

\textbf{Liaisons doubles.} On calcule un \emph{couplage de cardinalité
maximale} sur le graphe carbone par l'algorithme de
Blossom~\cite{edmonds1965}, et chaque arête du couplage est marquée comme
liaison double. Les carbones non couverts par le couplage sont les sites
radicalaires.

\textbf{Ajout des hydrogènes.} Tout carbone du bord, c'est-à-dire de
degré $2$ dans le graphe carbone, possède une valence libre : on lui attache
un atome d'hydrogène, placé radialement vers l'extérieur du cycle auquel il
appartient, à une distance de $1{,}088$~\AA{} (longueur typique de la liaison
C--H).

\textbf{Export.} La géométrie complète (carbones et hydrogènes, avec
coordonnées) est exportée au format XYZ (un format parmi d'autres de
représentation de molécule), lisible par \xtb{}.

La géométrie ainsi produite est \emph{plane par construction} : tous les atomes
sont dans le plan $z = 0$. C'est l'étape de validation \xtb{} (section
suivante) qui révélera si la molécule est réellement plane à l'équilibre, ou
si l'optimisation la fait quitter le plan.

\subsubsection{Validation par \xtb{} : protocole d'optimisation déterministe}
\label{sec:md}

L'optimisation \xtb{} procède par descente de gradient. Si la géométrie
initiale est parfaitement plane, l'optimiseur reste piégé dans un
\textbf{minimum plat parasite}. Pour briser cette symétrie de façon
reproductible, nous appliquons une \textbf{perturbation analytique
déterministe} aux coordonnées $z$ avant toute optimisation : la
perturbation est une fonction pure des indices atomiques, sans tirage
aléatoire, ce qui rend l'ensemble du pipeline \emph{byte-déterministe}.
La formule, la commande \texttt{xtb --opt tight} et tous les détails
techniques sont donnés en annexe~\ref{app:detopt}.

\subsubsection{Test de planéité}
\label{sec:acp}

Une fois la géométrie optimisée par \xtb{}, il reste à décider si la
molécule est plane. Nous utilisons deux métriques complémentaires, dont
seule la seconde sert au verdict final.

\paragraph{Métrique géométrique globale (ACP).} La première métrique,
implémentée dès les premières versions du pipeline, applique une
\textbf{analyse en composantes principales} aux positions des
carbones, calcule la normale du plan moyen, et pour chaque cycle
l'angle de gauchissement entre la normale locale du cycle et la
normale globale. La quantité retenue, $\texttt{max\_angle\_deg}$, est
le maximum de ces angles sur tous les cycles. Cette métrique donne une
image globale de la courbure : une molécule en cuvette ou en selle
produit un \texttt{max\_angle\_deg} élevé. Les détails techniques
(diagonalisation de la covariance, formules) sont en
annexe~\ref{app:acp}.

\paragraph{Métrique chimique locale (angles dièdres).} En discussion
avec les chimistes (Denis Hagebaum-Reignier, Yannick Carissan), nous
avons constaté que la métrique précédente capturait bien la
\emph{courbure d'ensemble} mais surestimait l'écart à la planéité du
système $\pi$, qui n'est sensible qu'à des déformations
\emph{localisées entre liaisons voisines}. Nous calculons donc, pour
chaque suite de quatre atomes de carbone $A$--$B$--$C$--$D$ tous
connectés par des liaisons covalentes consécutives (trois liaisons en
chaîne), l'angle dièdre signé $\varphi(A,B,C,D) \in (-180^\circ,
180^\circ]$. Pour une molécule strictement plane, chaque dièdre vaut
$0^\circ$ ou $\pm 180^\circ$ : l'\emph{écart au plan} est donc
$\delta(\varphi) = \min(|\varphi|, 180^\circ - |\varphi|) \in [0,
90^\circ]$. La métrique retenue est le maximum~:
\[
  \texttt{max\_dihedral\_deg}
    \;=\; \max_{A\text{-}B\text{-}C\text{-}D} \delta\bigl(\varphi(A,B,C,D)\bigr).
\]

\paragraph{Critère de décision (verdict final).} Sur recommandation
des chimistes, la classification adopte \emph{trois niveaux} plutôt
qu'un seuil binaire, le seuil exact étant difficile à fixer dans
l'absolu et dépendant de chaque système~:
\[
  \boxed{
    \begin{aligned}
      \texttt{max\_dihedral\_deg} &< 10^\circ &&\Rightarrow \text{très plan,} \\
      10^\circ \le \texttt{max\_dihedral\_deg} &< 25^\circ &&\Rightarrow \text{acceptable,} \\
      \texttt{max\_dihedral\_deg} &\ge 25^\circ &&\Rightarrow \text{non plan.}
    \end{aligned}
  }
\]
Chaque solution reçoit ainsi un verdict parmi \textsc{très plan},
\textsc{acceptable}, \textsc{non plan} ou \textsc{échec \xtb{}}. La
plage intermédiaire « acceptable » reflète l'avis chimique~: même avec
un dièdre maximal de $20^\circ$, le composé reste raisonnable
chimiquement (encore conjugué, encore stable). Les statistiques aux
deux seuils $10^\circ$ et $25^\circ$ sont reportées séparément à la
section~\ref{sec:xp}, comme suggéré par les chimistes pour distinguer
les molécules \emph{légèrement} déformées (entre $10^\circ$ et
$25^\circ$) de celles \emph{fortement} déformées ($\ge 25^\circ$).

\subsection{Analyse électronique des structures}
\label{sec:analyse-elec}

Une fois les structures planes obtenues, nous avons développé un outil pour en
étudier les propriétés électroniques. Ces analyses sont \emph{statiques} : elles
opèrent directement sur le graphe carbone (sans nouveau calcul quantique) et
sont donc quasi instantanées.

\paragraph{Modèle CSP $\mathcal{M}_K$ pour les structures de Kekulé.}
On associe une variable booléenne $x_e \in \{0,1\}$ à chaque liaison $e \in E$
du graphe carbone ($x_e = 1$ signifie liaison double). La contrainte unique est~:
\begin{equation}
  \sum_{e \in \delta(v)} x_e = 1, \quad \forall v \in V.
  \tag{C-Kek}
\end{equation}
L'énumération de toutes les solutions de $\mathcal{M}_K$ produit l'intégralité
des structures de Kekulé. Pour les molécules sans couplage parfait (graphe
carbone à nombre impair de sommets ou topologie radicalaire --- cas fréquent
avec des cycles à $5$ ou $7$), on relaxe l'égalité en inégalité et l'on
maximise $\sum x_e$ ; les atomes non couverts à l'optimum sont les
\emph{sites radicalaires}.

\begin{figure}[h]
\centering
\fbox{\begin{minipage}[c][5cm][c]{0.75\linewidth}\centering
  \emph{[Capture du visualiseur en mode Kekulé --- à insérer]}
\end{minipage}}
\caption{Visualiseur des structures de Kekulé.}
\label{fig:visu-kekule}
\end{figure}

\paragraph{Modèle CSP $\mathcal{M}_C$ pour les couvertures de Clar.}
On étend $\mathcal{M}_K$ avec une variable booléenne $y_h \in \{0,1\}$ par
hexagone ($y_h = 1$ signifie que $h$ porte un sextet de Clar). La contrainte
unifiée par atome est~:
\begin{equation}
  \sum_{e \in \delta(v)} x_e \;+\; \sum_{h \in \mathcal{H}\,:\, v \in V(h)} y_h \;=\; 1,
  \quad \forall v \in V,
  \tag{C-Clar}
\end{equation}
et l'on maximise $Z = \sum_{h \in \mathcal{H}} y_h$. Cette contrainte capture
simultanément la vertex-disjonction des sextets (deux sextets partageant un
atome vioteraient l'égalité à $1$) et le couplage parfait du résidu (tout atome
hors sextet doit être couvert par exactement une double liaison). En posant
$y_h = 0$ pour tout $h$, on retrouve $\mathcal{M}_K$ comme cas particulier.
Pour les structures radicalaires, on relaxe (C-Clar) en inégalité et l'on
utilise un objectif lexicographique : minimiser d'abord le nombre de radicaux,
puis maximiser $Z$.

\begin{figure}[h]
\centering
\fbox{\begin{minipage}[c][5cm][c]{0.75\linewidth}\centering
  \emph{[Capture du visualiseur en mode Clar --- à insérer]}
\end{minipage}}
\caption{Visualiseur en mode Clar.}
\label{fig:visu-clar}
\end{figure}

\paragraph{Extension du RBO aux cycles $5$ et $7$.} La définition de
Pauling--Randić~\cite{randic}, reprise par Varet, ne concerne que les
hexagones. Notre contribution est de conserver la formule
$\mathrm{CBO}(C) = \sum_{e \in C} \mathrm{bo}(e)$ \emph{sommée sur tout
cycle quelle que soit sa taille}, en affichant le ratio
$\mathrm{CBO}/\mathrm{cbo_{max}}$ pour rester fidèle au contexte. Le cas
radicalaire (aucune Kekulé stricte) n'est pas défini au sens de
Pauling--Randić.

\begin{figure}[h]
\centering
\fbox{\begin{minipage}[c][5cm][c]{0.75\linewidth}\centering
  \emph{[Capture du visualiseur en mode RBO --- à insérer]}
\end{minipage}}
\caption{Visualiseur en mode RBO.}
\label{fig:visu-rbo}
\end{figure}

\begin{remarque}[Alignement avec la littérature]
Ce modèle coïncide avec celui de \texttt{pah-tools} et de BenzAI~\cite{varet2022}
pour les benzénoïdes. Notre apport est l'extension aux cycles impairs ($5$ et $7$)
via la relaxation radicalaire de (C-Clar) et le CBO normalisé~: les outils
existants imposent $\mathcal{H}$ = hexagones uniquement et ne définissent pas
le cas sans couplage parfait.
\end{remarque}



\subsection{Le designer : interface et pipeline intégré}
\label{sec:logiciel}

L'ensemble des étapes décrites jusqu'ici --- modèle PPC, reconstruction
3D, validation \xtb{}, test de planéité, analyse électronique --- est
intégré dans une application web unique, le \emph{designer}, qui sert à
la fois d'interface d'utilisation et d'infrastructure de campagne
expérimentale. Cette sous-section en donne un aperçu fonctionnel et
détaille l'environnement technique (versions, machines, garanties de
reproductibilité).

\subsubsection{Saisie de l'instance}

Un canevas hexagonal en SVG permet à l'utilisateur de \emph{dessiner} un
benzénoïde de départ : on clique sur les hexagones du pavage pour les
sélectionner, et le graphe dual sous-jacent est construit à la volée.
La saisie peut aussi se faire par \emph{importation} : un benzénoïde
déjà enregistré dans la base BenzDB (ou venant d'un fichier au format
texte adapté) peut être chargé directement, ce qui est utile pour
rejouer une campagne sur un corpus déjà fixé.

\begin{figure}[h]
\centering
\fbox{\begin{minipage}[c][5cm][c]{0.75\linewidth}\centering
  \emph{[Capture du designer en mode saisie d'instance --- à insérer]}
\end{minipage}}
\caption{Designer en mode saisie : canevas hexagonal pour dessiner un
benzénoïde ou choisir un benzenoide importé depuis BenzDB.}
\label{fig:designer-saisie}
\end{figure}

\subsubsection{Configuration des contraintes et exécution}

Une fois l'instance posée, l'utilisateur configure le solveur depuis un
panneau organisé en deux onglets. L'onglet \textbf{Préréglage} propose
quatre configurations canoniques, sélectionnables d'un clic, qui
correspondent exactement aux configurations étudiées
expérimentalement (section~\ref{sec:xp}) :
\begin{itemize}[leftmargin=1.4em]
  \item \textbf{C1 --- Baseline} : contraintes structurelles
    uniquement (C1+C2+C3+C5+C6).
  \item \textbf{C2 --- Pb1 + adj\_57} : C1 plus pentagones plafonnés au
    bord et appariement Stone--Wales obligatoire.
  \item \textbf{C3 --- Pb1 + adj\_57 + pas de $7$--$7$ adjacents} :
    durcissement local de la courbure (Gauss--Bonnet locale à zéro
    dans un rayon de $2$ dans \GD{}).
  \item \textbf{Ctopo --- Topologie complète} : C3 plus une blacklist
    de motifs de rayon~$2$ du graphe dual et un pré-filtre exigeant un
    squelette compact (au moins $4$ atomes péri-condensés). Recommandé
    pour les grands squelettes ($h_9$).
\end{itemize}
L'onglet \textbf{Avancé} expose individuellement chaque flag (Pb1,
adj\_57, tau\_gb, ctopo\_filter, etc.) pour les expérimentations
fines.

Un second panneau gère la \textbf{validation \xtb{}}. Le mode par
défaut est \emph{skip xTB} : le pipeline s'arrête après la
reconstruction 3D et produit directement les géométries planes
(non optimisées), ce qui est très rapide et suffit pour visualiser les
sorties du CSP. Pour valider la planéité physique, l'utilisateur bascule
sur \emph{det-opt} (le protocole déterministe décrit
section~\ref{sec:md}), ou plus rarement sur les modes \emph{md} et
\emph{multi-runs} conservés pour comparaison historique. Une option
permet aussi de comparer la planéité des solutions à celle du
benzénoïde d'entrée pur (tout-hexagones).

Le lancement d'une génération orchestre, derrière l'interface,
l'ensemble du pipeline : compilation du modèle en XCSP3, appel à
\ace{}, reconstruction 3D des solutions, validation \xtb{} (si
demandée), test de planéité par ACP et calcul des métriques
électroniques. Les résultats --- solutions, géométries XYZ compressées,
statistiques par campagne --- sont persistés dans une base SQLite, ce
qui rend chaque campagne facilement rejouable et comparable. Les
exécutions sur le grand corpus ($h \ge 7$, jusqu'à plusieurs milliers
de squelettes) sont distribuées sur le cluster décrit ci-dessous.

\subsubsection{Exploration des solutions et visualiseur intégré}

Une fois une campagne terminée, le designer présente l'ensemble des
solutions produites sous forme d'une \textbf{liste paginée}
(figure~\ref{fig:designer-list}). Chaque ligne récapitule les
caractéristiques d'une solution : identifiant, composition
$(n_5, n_6, n_7)$, verdict de planéité, angle maximal hors-plan, et
distances quadratique / extrême au plan moyen.

Plusieurs leviers facilitent l'exploration :
\begin{itemize}
  \item \textbf{Tri} sur n'importe quelle colonne (angle croissant ou
    décroissant, identifiant, etc.) ;
  \item \textbf{Filtrage} par verdict de planéité (\textsc{plan},
    \textsc{non-plan}, géométrie infaisable, échec \xtb{}), par
    sous-chaîne de la composition, ou par squelette de départ ;
  \item navigation directe vers le \textbf{visualiseur 3D} d'une
    solution donnée pour inspecter sa géométrie en relief, lire ses
    structures de Kekulé, ses couvertures de Clar et ses RBO/CBO
    (section~\ref{sec:analyse-elec}).
\end{itemize}

Le visualiseur, intégré à la même application web, est aussi accessible
indépendamment du designer pour explorer la base de résultats du run
final ($\sim$$1{,}5$~M de solutions sur $h_3$--$h_9$). Il sert à la fois
d'outil d'exploration pour les chimistes et de banc de débogage pour le
développement (vérifier en 3D qu'une géométrie produite par le pipeline
ressemble à ce que l'on attend).

\begin{figure}[h]
\centering
\fbox{\begin{minipage}[c][5cm][c]{0.75\linewidth}\centering
  \emph{[Capture de la liste des solutions dans le designer --- à insérer]}
\end{minipage}}
\caption{Liste des solutions avec tri et filtrage.}
\label{fig:designer-list}
\end{figure}

% (Reproductibilité et environnement déplacés dans la section expérimentale --
%  section~\ref{sec:repro}.)


% =====================================================================
\section{Étude expérimentale : quelles contraintes améliorent la planéité ?}
\label{sec:xp}
% =====================================================================

Comme établi en section~\ref{sec:probleme}, la planéité est une propriété
globale qu'aucun jeu de contraintes locales ne capture exactement. La question
pratique devient alors : \emph{quelles contraintes additionnelles augmentent le
plus la proportion de molécules planes parmi celles que le modèle génère ?} Nous
y répondons par une étude comparative de quatre configurations $\mathbf{C_1}$,
$\mathbf{C_2}$, $\mathbf{C_3}$ et $\mathbf{C_{\text{topo}}}$, des plus simples
aux plus contraintes, qui isolent successivement chaque levier identifié.

\subsection{Protocole et banc d'essai}

Le banc d'essai utilise les squelettes benzénoïdes canoniques de la base
\emph{BenzDB}, de $h_3$ à $h_9$ hexagones (tableau~\ref{tab:corpus}). La
métrique principale est la \textbf{distribution des verdicts dièdres}
définis section~\ref{sec:acp} : pour chaque configuration, on reporte la
proportion de solutions \textbf{très planes} (\textbf{TP}, dièdre maximal
$< 10^\circ$) et \textbf{plan acceptable} (\textbf{A}, dièdre maximal $< 25^\circ$,
\emph{cumulatif} : la colonne~A inclut toutes les solutions très planes),
complétée par le \emph{nombre absolu} $N$ de solutions générées (souvent
plus parlant pour l'aval, qui s'intéresse au nombre de candidats
exploitables). Le complément à $100\,\%$ de la colonne A donne la
proportion de solutions \textsc{non planes} (dièdre $\ge 25^\circ$). La
distinction \textbf{TP} \emph{vs.}\ \textbf{A} suit la recommandation des
chimistes (Denis Hagebaum-Reignier) : un dièdre maximal entre 10 et
$25^\circ$ correspond à une molécule \emph{légèrement} déformée qui reste
chimiquement raisonnable, à distinguer d'une planéité quasi parfaite. Les
campagnes sont reproductibles (validation \emph{det-opt},
section~\ref{sec:md}), agrégées en base SQLite, et chaque configuration
de contraintes est comparée terme à terme aux autres sur le même corpus.

\begin{table}[h]
\centering
\begin{tabular}{lccccccc}
\toprule
$h$ & $h_3$ & $h_4$ & $h_5$ & $h_6$ & $h_7$ & $h_8$ & $h_9$ \\
\midrule
nombre de squelettes & $3$ & $6$ & $16$ & $44$ & $126$ & $376$ & $2\,418$ \\
\bottomrule
\end{tabular}
\caption{Nombre de squelettes benzénoïdes du corpus par taille $h$, issue de BenzDB.}
\label{tab:corpus}
\end{table}

\subsection{Environnement logiciel et reproductibilité}
\label{sec:repro}

La reproductibilité des expériences est une garantie centrale du
pipeline : un même fichier d'entrée et une même configuration de
contraintes doivent produire le \emph{même} résultat, indépendamment de la machine ou de la date d'exécution.

\paragraph{Stack logicielle.}
Le pipeline est écrit en \textbf{Python 3.14.4}, aligné entre le poste
de développement (Windows~11) et l'environnement \texttt{conda} du
cluster. Les bibliothèques utilisées et leurs versions (extraits de
\texttt{requirements.txt}) sont récapitulées dans le
tableau~\ref{tab:stack}.

\begin{table}[!htbp]
\centering
\small
\begin{tabular}{lll}
\toprule
\textbf{Bibliothèque} & \textbf{Version} & \textbf{Rôle} \\
\midrule
Python      & 3.14.4 & langage hôte du pipeline \\
PyCSP3      & 2.5.1  & modélisation des contraintes (génération XCSP3) \\
ACE         & ---    & solveur CSP Java invoqué par PyCSP3 \\
NetworkX    & 3.6.1  & graphes (dual, carbone, couplage de Blossom) \\
NumPy       & 2.4.6  & algèbre linéaire (ACP, reconstruction 3D) \\
Flask       & 3.1.3  & backend web du designer \\
Werkzeug    & 3.1.8  & dépendance Flask (WSGI) \\
Jinja2      & 3.1.6  & dépendance Flask (templates) \\
3Dmol.js    & ---    & bibliothèque JS du visualiseur 3D (via CDN) \\
\bottomrule
\end{tabular}
\caption{Stack logicielle du pipeline et du designer.}
\label{tab:stack}
\end{table}

\paragraph{Cluster de calcul.}
Les campagnes de grande taille tournent sur le cluster \textbf{COALA}
du LIS (Aix-Marseille Université), composé de \textbf{16 nœuds}
(lames \texttt{lis-cluster-coala-49} à \texttt{-64}), chacun équipé
de \textbf{2 processeurs Intel Xeon Gold 5218R}
($20$ cœurs physiques) et \textbf{187 GiB de RAM}.
Au total, jusqu'à \textbf{320 instances \xtb{} en parallèle}. Les
artefacts (solutions, géométries XYZ, verdicts) sont persistés dans
une base \textbf{SQLite} (mode \texttt{WAL}). Les détails du protocole
(variables d'environnement, \texttt{PYTHONHASHSEED}, orchestration
cluster) sont en annexe~\ref{app:detopt}.

\subsection{$\mathbf{C_1}$ --- Baseline (contraintes structurelles seules)}

La première configuration mesure la référence : avec les seules contraintes
structurelles (section~\ref{sec:modele}) --- conservation du carbone C1, gel
des hexagones de degré $6$ (C3), table de voisinage (C5) et rupture de
symétrie (C4) ---, quelle proportion des solutions est réellement plane après
validation \xtb{}~?

Le tableau~\ref{tab:res-c1} récapitule les résultats sur le corpus
$h_3$--$h_9$.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
$h$ & $N$ & $N_{\text{TP}}$ & \%TP & $N_{\text{A}}$ & \%A \\
\midrule
$h_3$ & $7$        & $7$        & $100{,}0\,\%$ & $7$        & $100{,}0\,\%$ \\
$h_4$ & $44$       & $42$       & $95{,}5\,\%$  & $42$       & $95{,}5\,\%$  \\
$h_5$ & $278$      & $245$      & $88{,}1\,\%$  & $252$      & $90{,}6\,\%$  \\
$h_6$ & $2\,146$   & $1\,691$   & $78{,}8\,\%$  & $1\,834$   & $85{,}5\,\%$  \\
$h_7$ & $15\,004$  & $9\,909$   & $66{,}0\,\%$  & $11\,381$  & $75{,}8\,\%$  \\
$h_8$ & $112\,800$ & $64\,677$  & $57{,}3\,\%$  & $78\,793$  & $69{,}9\,\%$  \\
$h_9$ & $646\,319$ & $241\,577$ & $37{,}4\,\%$  & $351\,343$ & $54{,}4\,\%$  \\
\bottomrule
\end{tabular}
\caption{Résultats de la configuration $C_1$ (baseline). $N_{\text{TP}}$ :
nombre de solutions très planes (dièdre $< 10^\circ$). $N_{\text{A}}$ :
nombre cumulé de solutions planes ou acceptables (dièdre $< 25^\circ$),
incluant $N_{\text{TP}}$.}
\label{tab:res-c1}
\end{table}

\subsection{$\mathbf{C_2}$ --- Pb1 + adjacence $5$--$7$ (motif Stone--Wales)}

$C_2$ ajoute à $C_1$ deux contraintes complémentaires, identifiées comme les
plus efficaces parmi les variantes locales explorées :
\begin{itemize}[leftmargin=1.4em]
  \item \textbf{Pb1} (\texttt{K\_pb=1}) :
    $\sum_{v \in \partial \GD} \mathbf{1}[x_v = 5] \le 1$, c'est-à-dire
    \emph{au plus un pentagone au bord} du squelette. Cette contrainte traduit
    l'observation empirique que les pentagones de bord, qui introduisent une
    courbure locale non compensée par un voisin opposé, sont particulièrement
    déstabilisants ; la limiter améliore fortement le taux de planéité.
  \item \textbf{adj\_57} (contrainte C7, section~\ref{sec:c7}) :
    appariement Stone--Wales obligatoire (tout pentagone a un heptagone
    voisin, et réciproquement). Localement, la paire $5$--$7$ compense
    exactement la courbure de Gauss--Bonnet ($+\pi/3 - \pi/3 = 0$).
\end{itemize}
Une troisième contrainte testée parallèlement, $|n_5 - n_7| \le K_{\text{sym}}$
(notée C-SYM), s'est révélée \emph{sans effet mesurable} : l'équilibre
$n_5 = n_7$ est déjà imposé par C1, et le filtrage par la table produit
naturellement des compositions équilibrées.

Le tableau~\ref{tab:res-c2} récapitule les résultats sur le corpus.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
$h$ & $N$ & $N_{\text{TP}}$ & \%TP & $N_{\text{A}}$ & \%A \\
\midrule
$h_3$ & $5$       & $5$       & $100{,}0\,\%$ & $5$       & $100{,}0\,\%$ \\
$h_4$ & $16$      & $16$      & $100{,}0\,\%$ & $16$      & $100{,}0\,\%$ \\
$h_5$ & $62$      & $55$      & $88{,}7\,\%$  & $55$      & $88{,}7\,\%$  \\
$h_6$ & $254$     & $224$     & $88{,}2\,\%$  & $233$     & $91{,}7\,\%$  \\
$h_7$ & $900$     & $749$     & $83{,}2\,\%$  & $802$     & $89{,}1\,\%$  \\
$h_8$ & $3\,292$  & $2\,669$  & $81{,}1\,\%$  & $2\,925$  & $88{,}8\,\%$  \\
$h_9$ & $21\,127$ & $10\,170$ & $48{,}1\,\%$  & $15\,623$ & $73{,}9\,\%$  \\
\bottomrule
\end{tabular}
\caption{Résultats de la configuration $C_2$ (Pb1 + adj\_57). Colonnes :
voir tableau~\ref{tab:res-c1}.}
\label{tab:res-c2}
\end{table}

\subsection{$\mathbf{C_3}$ --- Interdire les paires $7$--$7$ adjacentes}

$C_3$ durcit $C_2$ en interdisant la concentration locale de courbure
négative. Concrètement, on impose une \textbf{contrainte de Gauss--Bonnet
locale} de paramètre $\tau_{\text{gb}} = 0$ et rayon $2$ dans le graphe dual :
\[
  \forall v \in V_D,\quad
  \bigl|\,|\{u : x_u = 5, \mathrm{d}_{\GD}(u,v) \le 2\}|
       - |\{u : x_u = 7, \mathrm{d}_{\GD}(u,v) \le 2\}|\,\bigr| \le 0 .
\]
En pratique, cela revient à interdire que deux heptagones soient à distance
$\le 2$ dans le dual sans être compensés par autant de pentagones dans le
même voisinage, ce qui exclut notamment les paires $7$--$7$ adjacentes.

Le tableau~\ref{tab:res-c3} récapitule les résultats sur le corpus.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
$h$ & $N$ & $N_{\text{TP}}$ & \%TP & $N_{\text{A}}$ & \%A \\
\midrule
$h_3$ & $5$      & $5$      & $100{,}0\,\%$ & $5$      & $100{,}0\,\%$ \\
$h_4$ & $12$     & $12$     & $100{,}0\,\%$ & $12$     & $100{,}0\,\%$ \\
$h_5$ & $28$     & $24$     & $85{,}7\,\%$  & $24$     & $85{,}7\,\%$  \\
$h_6$ & $75$     & $70$     & $93{,}3\,\%$  & $71$     & $94{,}7\,\%$  \\
$h_7$ & $178$    & $170$    & $95{,}5\,\%$  & $171$    & $96{,}1\,\%$  \\
$h_8$ & $512$    & $498$    & $97{,}3\,\%$  & $500$    & $97{,}7\,\%$  \\
$h_9$ & $2\,845$ & $1\,696$ & $59{,}6\,\%$  & $2\,292$ & $80{,}5\,\%$  \\
\bottomrule
\end{tabular}
\caption{Résultats de la configuration $C_3$ (Gauss--Bonnet local, rayon~2).
Colonnes : voir tableau~\ref{tab:res-c1}.}
\label{tab:res-c3}
\end{table}

\subsection{$\mathbf{C_{\text{topo}}}$ --- Topologie complète}
\label{sec:ctopo}

Pour traiter $h_9$, nous avons cherché des contraintes \emph{non
redondantes} avec Pb1 et qui capturent un mécanisme d'échec différent. Une
analyse \emph{a posteriori} des solutions $C_1$ non planes a fait émerger
deux \textbf{descripteurs topologiques} complémentaires, qui forment ensemble la
configuration $C_{\text{topo}}$. On appelle ici \emph{descripteur topologique}
toute quantité calculée sur le graphe dual d'une solution qui résume une
propriété structurelle et dont on observe la corrélation avec la planéité.

\paragraph{Descripteur 1 --- motifs de rayon~$2$ dans le graphe dual.}
Pour chaque cycle $v$ d'une solution, on définit son \emph{motif de
rayon~$2$} comme le couple
$(x_v,\ \mathcal{M}(\,x_u : u \in \mathcal{N}_{\GD}(v)\,))$,
où $\mathcal{M}(\cdot)$ désigne un \textbf{multiset} (ou \emph{multi-ensemble}),
c'est-à-dire un ensemble autorisant les répétitions : $\mathcal{M}(6,7,7)
\neq \mathcal{M}(6,7)$, les deux voisins de taille~$7$ étant comptés
séparément.
Par exemple, le motif $7\,|\,[6,7,7]$ désigne un heptagone entouré d'un
hexagone et de deux heptagones. L'analyse statistique sur le corpus $h_7$,
$h_8$, $h_9$ a identifié une \emph{blacklist} d'une dizaine de motifs
fortement corrélés à la non-planéité, indépendamment de la taille du
benzénoïde : par exemple $7\,|\,[6,7,7]$ ne produit que $9{,}2\,\%$ de
solutions planes sur $h_8$, contre une moyenne de $\sim$$60\,\%$. La
contrainte $C_{\text{topo}}$ interdit explicitement tous ces motifs ;
elle est implémentée par une famille de contraintes négatives sur les
tuples $(x_v, x_{u_1}, \dots, x_{u_k})$, intégrées dans le modèle PPC.

\paragraph{Descripteur 2 --- topologie du squelette.}
Le second descripteur est calculé \emph{avant} l'assignment des tailles :
le nombre $n_{\text{péri}}$ d'atomes péri-condensés du squelette, c'est-à-dire
partagés par au moins trois cycles. Empiriquement, les squelettes
$n_{\text{péri}} \ge 4$ (péri-condensés, type coronène) résistent
beaucoup mieux à la déformation que les squelettes étalés, où les défauts
peuvent se replier sans coût énergétique majeur. La contrainte impose donc
$n_{\text{péri}} \ge 4$, ce qui est un \emph{pré-filtre sur le squelette
d'entrée} : si le benzénoïde fourni ne le satisfait pas, $C_{\text{topo}}$
ne génère aucune solution.

\paragraph{Résultats.}
Le tableau~\ref{tab:res-ctopo} récapitule les résultats sur le corpus.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
$h$ & $N$ & $N_{\text{TP}}$ & \%TP & $N_{\text{A}}$ & \%A \\
\midrule
$h_6$ & $90$      & $90$      & $100{,}0\,\%$ & $90$      & $100{,}0\,\%$ \\
$h_7$ & $1\,035$  & $980$     & $94{,}7\,\%$  & $998$     & $96{,}4\,\%$  \\
$h_8$ & $9\,340$  & $7\,989$  & $85{,}5\,\%$  & $8\,512$  & $91{,}1\,\%$  \\
$h_9$ & $43\,057$ & $29\,954$ & $69{,}6\,\%$  & $35\,440$ & $82{,}3\,\%$  \\
\bottomrule
\end{tabular}
\caption{Résultats de la configuration $C_{\text{topo}}$ (topologie complète,
applicable à partir de $h_6$). Colonnes : voir tableau~\ref{tab:res-c1}.}
\label{tab:res-ctopo}
\end{table}

\subsection{Bilan expérimental}

L'étude désigne trois régimes selon la taille du squelette d'entrée.

\begin{itemize}[leftmargin=1.4em]
  \item \textbf{Pour $h_3$--$h_5$ :} les volumes restent faibles ($\le 280$
    solutions par configuration sur tout le corpus), le taux de très planes
    de $C_1$ seul est déjà élevé ($88$--$100\,\%$), et $C_2$ ou $C_3$
    atteignent typiquement $100\,\%$ très planes. À cette échelle, le
    filtrage par contraintes additionnelles est essentiellement superflu,
    et la configuration $C_{\text{topo}}$ n'est pas applicable (le
    pré-filtre $n_{\text{péri}} \ge 4$ exclut tous les squelettes,
    $n_{\text{péri}}$ ne dépassant pas $3$ avant $h_6$) ;
  \item \textbf{Pour $h_6$--$h_8$ :} $C_3$ reste la plus pure
    ($93$--$\mathbf{97{,}3}\,\%$ très planes), au prix d'un volume réduit ;
    à utiliser lorsqu'on cherche très peu de candidats mais très fiables.
    $C_{\text{topo}}$ atteint la même couverture cumulée \%A en élargissant
    le volume d'un facteur $\sim 10$ ;
  \item \textbf{Pour $h_9$ et au-delà :} $C_{\text{topo}}$ est nettement
    supérieure (\textbf{$69{,}6\,\%$} de très planes contre $59{,}6\,\%$
    pour $C_3$, $\mathbf{82{,}3\,\%}$ cumulé acceptable contre
    $80{,}5\,\%$), \emph{et} produit dix-huit fois plus de très planes en
    absolu ($29\,954$ contre $1\,696$). C'est la configuration par défaut
    recommandée pour les grandes tailles.
\end{itemize}

\paragraph{Lecture chimique de la zone « acceptable ».} La métrique cumulée~A
(dièdre $< 25^\circ$) montre que la grande majorité des solutions de $C_1$
non strictement planes restent dans une plage modérément déformée, en
particulier sur $h_9$ : $54{,}4\,\%$ contre $37{,}4\,\%$ pour le strict
$< 10^\circ$, soit un écart de $+17$ points. Sur les configurations
contraintes, l'écart est plus modeste ($+1$ à $+13$ points) car les
solutions sont déjà majoritairement très planes. Cela suggère, comme
Denis Hagebaum-Reignier l'indique, que les molécules issues du modèle
qui « se déforment » le font \emph{modérément} (entre $10$ et $25^\circ$)
plutôt que franchement. Une analyse statistique sur des
non-benzénoïdes synthétisés permettrait de raffiner ce seuil ; ce travail
dépasse le cadre du stage.

L'effet $C_{\text{topo}}$ illustre une difficulté méthodologique
identifiée à la section~\ref{sec:probleme} : la planéité est une
propriété \emph{globale}, et les leviers les plus efficaces ne s'expriment
pas comme contraintes locales \og naturelles \fg{} sur les tailles
$5/6/7$. Ils nécessitent d'une part une exploration statistique du
corpus $C_1$ pour identifier des motifs déstabilisants (descripteur~1,
\emph{a posteriori}), et d'autre part une caractérisation du squelette
\emph{avant assignment} (descripteur~2, indépendant de la solution).
Cette démarche --- générer un corpus, analyser ses échecs, en extraire
des contraintes, ré-énumérer --- préfigure une boucle d'apprentissage
que nous discutons en perspective (section~\ref{sec:conclusion}).

% =====================================================================
\section{Conclusion et perspectives}
\label{sec:conclusion}
% =====================================================================

\subsection{Conclusion}

Ce stage a étendu aux non-benzénoïdes le cadre de génération exhaustive par
programmation par contraintes proposé par Varet~\cite{varet2022}. Nous avons
proposé un modèle PPC qui assigne à chaque hexagone d'un benzénoïde d'entrée
une taille dans $\{5, 6, 7\}$, articulé autour d'une \emph{table de voisinage}
extensionnelle de $721$ entrées sensible à la disposition des voisins, et
d'une rupture de symétrie par lex-leader sur le groupe d'automorphismes du
graphe dual. Chaque solution du modèle est reconstruite en géométrie 3D, puis
validée par un pipeline \xtb{}+ACP, dont le protocole d'optimisation
déterministe (perturbation analytique en $\sin(2\pi i / N + \varphi)$ suivie
de \texttt{xtb --opt} en mono-thread) garantit la reproductibilité
byte-pour-byte, contrairement à la dynamique moléculaire initialement
employée. L'ensemble est intégré dans une application web --- le
\emph{designer} et son visualiseur 3D --- qui étend également aux cycles à
$5$ et $7$ atomes l'analyse électronique (structures de Kekulé, couvertures
de Clar, ring bond order), restreinte jusqu'ici aux hexagones.

L'étude expérimentale sur les squelettes $h_3$--$h_9$ de BenzDB a comparé
quatre configurations, du modèle de base $C_1$ à la configuration
$C_{\text{topo}}$ combinant une blacklist de motifs de rayon~$2$ et un
pré-filtre sur le squelette ($n_{\text{péri}} \ge 4$). Suivant la
recommandation des chimistes, l'évaluation utilise la métrique \emph{angle
dièdre maximal entre liaisons connectées}, plus fidèle à la planéité du
système conjugué que la métrique ACP globale, avec deux seuils : très plan
($< 10^\circ$) et acceptable ($< 25^\circ$). Les configurations purement
locales $C_2$ et $C_3$ atteignent une pureté élevée sur les tailles
intermédiaires (jusqu'à $\mathbf{97{,}3\,\%}$ de très planes sur $h_8$)
mais s'effondrent sur $h_9$ ($59{,}6\,\%$ très planes pour $C_3$). Sur
cette grande taille, $C_{\text{topo}}$ s'impose nettement : $69{,}6\,\%$ de
très planes et $\mathbf{82{,}3\,\%}$ une fois la zone acceptable
incluse, soit \emph{dix-huit fois plus} de très planes en absolu que
$C_3$ ($29\,954$ contre $1\,696$). Ce résultat confirme expérimentalement
ce que nous avions identifié théoriquement~: la planéité est une propriété
globale qui ne se capture pas, sur les grands squelettes, par des
contraintes purement locales sur les tailles $5/6/7$. Les leviers les
plus efficaces nécessitent soit une analyse statistique \emph{a posteriori}
du corpus généré (descripteurs de rayon~$2$), soit un pré-filtre sur le
squelette d'entrée indépendant de l'assignment ($n_{\text{péri}}$) ---
difficulté qui motive directement les perspectives ci-après.

\subsection{Perspectives}

La démarche suivie pendant ce stage présente une forme caractéristique : on
génère un corpus de candidats par un modèle PPC, on les valide par \xtb{}, on
observe les résultats, on interprète les régularités (les pentagones de bord
nuisent à la planéité, les paires $5$--$7$ se compensent), on les transforme
en hypothèses, on les exprime sous forme de nouvelles contraintes, puis on
ré-énumère pour mesurer leur effet. Au sens méthodologique, c'est une boucle
d'\emph{apprentissage} aujourd'hui tenue par l'humain ; demain, elle pourrait
être tenue partiellement par une machine. C'est précisément l'enjeu de la
\textbf{combinaison IA symbolique (PPC) \,+\, IA d'apprentissage}, qui fait
l'objet d'une recherche active actuellement~\cite{cappart2023,bengio2021}.
La PPC apporte la
garantie d'exhaustivité et l'explicabilité (chaque solution satisfait des
règles écrites explicitement) ; l'apprentissage apporte la capacité à inférer
des motifs dans des espaces de grande dimension où l'humain ne saurait les
formuler à la main. Pour notre problème, cela ouvre plusieurs voies :
apprendre des contraintes à partir du corpus déjà généré, guider la recherche
du solveur par une heuristique entraînée, ou encore prédire la planéité par
un réseau de neurones sur graphe pour éviter les appels \xtb{} les moins
informatifs. Cette direction, qui maintient le moteur symbolique au cœur
mais l'enrichit d'heuristiques apprises, semble particulièrement adaptée à un
problème comme le nôtre, où la connaissance chimique est encore en
construction et progresse au rythme des expériences elles-mêmes.

% =====================================================================
%  ANNEXES  (hors limite des 30 pages)
% =====================================================================
\newpage
\appendix

\section{Annexe}

\subsection{Définitions formelles de cohérence}
\label{app:coherence}

Les définitions suivantes sont adaptées de Mackworth~\cite{mackworth1977}
et reprises dans la thèse de Varet~\cite{varet2022} (Déf.\ 1.4.1).

\begin{definition}[Arc-cohérence]
Soit $P = (X, D, C)$ un CSP. Une valeur $v \in D_x$ est
\emph{arc-cohérente} par rapport à une contrainte $c \in C$ de portée
$S(c) \ni x$ si, pour chaque autre variable $y \in S(c)$, il existe une
valeur $w \in D_y$ telle que le tuple partiellement défini par
$t[\{x\}] = v$, $t[\{y\}] = w$ est compatible avec $R(c)$.

Un CSP est dit \emph{arc-cohérent} si chaque valeur de chaque domaine
est arc-cohérente par rapport à toutes les contraintes qui la concernent.
\end{definition}

\begin{definition}[Propagation de contraintes]
La \emph{propagation de contraintes} (ou \emph{filtrage}) est le
processus itératif consistant à supprimer des domaines toute valeur non
arc-cohérente. Le processus se répète jusqu'à l'obtention d'un
\emph{point fixe} : un état où aucune suppression supplémentaire n'est
possible. Chaque suppression peut déclencher de nouvelles suppressions
en cascade (effet ``boule de neige'').
\end{definition}

\begin{remarque}
L'arc-cohérence ne garantit pas à elle seule la satisfiabilité du CSP :
un problème peut être arc-cohérent et ne posséder aucune solution (les
domaines restent non vides, mais aucune combinaison complète n'est
consistante). Elle réduit cependant l'espace de recherche et est
systématiquement appliquée avant et pendant le backtracking.
\end{remarque}

% ---------------------------------------------------------------
\subsection{Règle de Hückel}
\label{app:huckel}
% ---------------------------------------------------------------

La règle suivante est établie par Hückel en 1931~\cite{huckel1931} dans le
cadre de la théorie des orbitales moléculaires au niveau \emph{tight-binding}.

\begin{definition}[Règle de Hückel, \cite{huckel1931}]
Un cycle conjugué plan est \emph{aromatique} si et seulement s'il
possède $4n + 2$ électrons $\pi$ pour un entier $n \ge 0$.
\end{definition}

Les premiers cas sont : $n = 0$ ($2$ électrons, cyclopropényle cation),
$n = 1$ ($6$ électrons, benzène), $n = 2$ ($10$ électrons, azulène et
naphtalène). Les cycles à $4n$ électrons (cyclobutadiène~: $4$, cyclo-octatetraène~:
$8$) sont dits \emph{antiaromatiques} et sont déstabilisés.

\begin{remarque}
Dans notre contexte, seuls les hexagones satisfont la règle avec $n = 1$
(6 atomes de carbone $\Rightarrow$ 6 électrons $\pi$ délocalisés). Les
pentagones et heptagones n'ont pas le bon décompte et ne peuvent donc
pas porter de sextet de Clar. C'est pourquoi la variable $y_h$ du
modèle $\mathcal{M}_C$ (section~\ref{sec:analyse-elec}) n'est définie
que sur les hexagones $h \in \mathcal{H}$.
\end{remarque}

% ---------------------------------------------------------------
\subsection{Protocole de validation \xtb{} (détails)}
\label{app:detopt}
% ---------------------------------------------------------------

\paragraph{Historique : abandon de la dynamique moléculaire.}
Une première version du pipeline utilisait une \emph{dynamique
moléculaire} courte (\texttt{xtb --md} à $298$~K pendant $1$~ps) pour
briser la symétrie planaire parasite avant l'optimisation. Cette
approche s'est révélée \textbf{non reproductible} : \xtb{} initialise
les vitesses MD à partir de l'horloge système et n'expose aucune graine
accessible (confirmé par l'issue GitHub \#$730$ du dépôt
\texttt{grimme-lab/xtb}, ouverte depuis $2022$ et sans réponse). Sur
des structures tendues ($h_8$--$h_9$), deux exécutions identiques
pouvaient basculer entre les verdicts \textsc{plan} et
\textsc{non-plan}, ce qui invalidait toute campagne expérimentale.

\paragraph{Protocole déterministe retenu.}
La perturbation $z_i' = z_i + A\sin(2\pi i/N + \varphi)$ ($A=0{,}05$~\AA,
$\varphi=0{,}5$~rad) est appliquée aux $N$ atomes ordonnés
canoniquement avant l'appel à \texttt{xtb --opt tight}
(GFN2-\xtb{} 6.7.1). Les variables d'environnement suivantes sont
imposées avant chaque appel :
\begin{quote}\small\ttfamily
OMP\_NUM\_THREADS=1 \quad MKL\_NUM\_THREADS=1 \\
OPENBLAS\_NUM\_THREADS=1 \quad NUMEXPR\_NUM\_THREADS=1 \\
PYTHONHASHSEED=0
\end{quote}
De plus, l'ordre canonique \texttt{sorted(\dots)} est appliqué partout
où des collections non ordonnées (ensembles d'atomes, de liaisons)
seraient itérées. L'ensemble du pipeline est ainsi
\emph{byte-déterministe}.

\paragraph{Filtre anti-fragmentation.}
Un filtre rejette les optimisations où un atome s'éjecte au-delà de
$30$~\AA{} du barycentre (liaison rompue). La solution reçoit alors le
verdict \textsc{échec \xtb{}}.

\paragraph{Persistance.}
La géométrie finale est conservée dans \texttt{md\_final\_opt.xyz} sous
chaque solution. L'orchestration sur le cluster COALA utilise un
dispatcher basé sur un \emph{manifest JSONL} partagé sur NFS et des
verrous atomiques (\texttt{O\_CREAT|O\_EXCL}) garantissant qu'un job
n'est pris que par un seul worker.

% ---------------------------------------------------------------
\subsection{Test de planéité par ACP (détails)}
\label{app:acp}
% ---------------------------------------------------------------

\paragraph{Calcul du plan moyen.}
Soit $\mathbf{x}_1, \dots, \mathbf{x}_N \in \mathbb{R}^3$ les
coordonnées des carbones après optimisation \xtb{}. On centre le nuage
($\bar{\mathbf{x}} = \frac{1}{N}\sum_i \mathbf{x}_i$) et on forme la
matrice de covariance $3\times 3$


\[
  \Sigma = \frac{1}{N}\sum_{i=1}^{N}
    (\mathbf{x}_i - \bar{\mathbf{x}})(\mathbf{x}_i - \bar{\mathbf{x}})^\top .
\]


La diagonalisation de $\Sigma$ fournit trois vecteurs propres
orthonormés $\mathbf{v}_1, \mathbf{v}_2, \mathbf{v}_3$ avec valeurs
propres $\lambda_1 \ge \lambda_2 \ge \lambda_3 \ge 0$. Le plan moyen
est engendré par $\mathbf{v}_1, \mathbf{v}_2$ ; $\mathbf{v}_3$ est sa
\textbf{normale globale}.

\paragraph{Métriques globales.}
\begin{itemize}[leftmargin=1.4em]
  \item \emph{rmsd plan} :
    $\sqrt{\tfrac{1}{N}\sum_i (\mathbf{v}_3^\top(\mathbf{x}_i -
    \bar{\mathbf{x}}))^2}$ --- épaisseur quadratique moyenne ;
  \item \emph{hauteur} : amplitude
    $\max_i - \min_i$ des projections sur $\mathbf{v}_3$ --- épaisseur
    extrême ;
  \item angle entre $\mathbf{v}_3$ et l'axe $Z$ global --- valeur
    diagnostique (inclinaison d'ensemble sans déformation).
\end{itemize}

\paragraph{Métrique \texttt{max\_angle\_deg}.}
Pour chaque cycle $C$, la même ACP sur ses seuls sommets donne une
normale locale $\mathbf{n}_C$. La valeur maximale de l'angle de
gauchissement est


\[
  \texttt{max\_angle\_deg} = \max_{C} \arccos(|\mathbf{n}_C \cdot
  \mathbf{v}_3|).
\]


Cette quantité, utilisée historiquement comme critère de planéité
($\texttt{max\_angle\_deg} \le 10^\circ$), est conservée dans la base
de résultats pour comparaison. Pour le verdict final, elle est
remplacée par \texttt{max\_dihedral\_deg} (voir ci-dessous), plus
fidèle à l'évaluation chimique de la planéité.

% ---------------------------------------------------------------
\subsection{Calcul de la métrique dièdre \texttt{max\_dihedral\_deg}}
\label{app:dihedral}
% ---------------------------------------------------------------

\paragraph{Énumération des chemins.}
Soit $G_C = (V_C, E_C)$ le graphe carbone après optimisation \xtb{}
(les hydrogènes sont écartés). Pour chaque arête $\{B,C\} \in E_C$
prise dans l'ordre $B < C$, et pour chaque paire de voisins
$A \in \mathcal{N}_{G_C}(B)$, $D \in \mathcal{N}_{G_C}(C)$ vérifiant
$A \ne C$, $D \ne B$ et $A \ne D$, le quadruplet $(A,B,C,D)$ forme
une chaîne de trois liaisons consécutives.

\paragraph{Angle dièdre.}
Pour chaque quadruplet, posons
$\mathbf{b}_1 = \mathbf{x}_B - \mathbf{x}_A$,
$\mathbf{b}_2 = \mathbf{x}_C - \mathbf{x}_B$,
$\mathbf{b}_3 = \mathbf{x}_D - \mathbf{x}_C$.
L'angle dièdre signé entre les plans $(A,B,C)$ et $(B,C,D)$ est
\[
  \varphi(A,B,C,D) \;=\; \operatorname{atan2}\bigl(
    \|\mathbf{b}_2\| \cdot (\mathbf{b}_1 \cdot (\mathbf{b}_2 \times \mathbf{b}_3)),
    \;
    (\mathbf{b}_1 \times \mathbf{b}_2) \cdot (\mathbf{b}_2 \times \mathbf{b}_3)
  \bigr) \in (-180^\circ, 180^\circ].
\]

\paragraph{Écart au plan.}
Pour une molécule plane, chaque $\varphi$ vaut exactement
$0^\circ$ (configuration \emph{cis}) ou $\pm 180^\circ$
(configuration \emph{trans}). L'écart au plan est donc
\[
  \delta(\varphi) \;=\; \min\bigl(|\varphi|,\; 180^\circ - |\varphi|\bigr)
  \;\in\; [0, 90^\circ],
\]
et la métrique retenue est le maximum sur tous les quadruplets
connectés. Une molécule parfaitement plane vérifie
$\texttt{max\_dihedral\_deg} = 0^\circ$ ; une molécule fortement
courbée s'approche de $90^\circ$.

\paragraph{Coût.}
Implémentation vectorielle en NumPy : $\sim 0{,}4$~ms par molécule.
Le calcul sur l'intégralité de la base finale ($\sim 805\,000$
géométries optimisées) prend environ $5$ minutes en mono-thread, sans
nouvelle invocation de \xtb{}.

% =====================================================================
%  BIBLIOGRAPHIE
% =====================================================================
\newpage
\renewcommand{\refname}{Références et bibliographie}
\begin{thebibliography}{99}
\small

\bibitem{varet2022} A.~Varet,
\emph{Programmation par contraintes et chimie théorique : utilisation du
formalisme CSP pour résoudre des problématiques liées aux benzénoïdes},
thèse de doctorat, Aix-Marseille Université, 2022.

\bibitem{handbookcp} F.~Rossi, P.~van~Beek, T.~Walsh (éds.),
\emph{Handbook of Constraint Programming}, Elsevier, 2006.

\bibitem{lecoutre2009} C.~Lecoutre,
\emph{Constraint Networks: Techniques and Algorithms}, ISTE/Wiley, 2009.

\bibitem{pycsp3ace} C.~Lecoutre, N.~Szczepanski,
\emph{PyCSP3 : modélisation en programmation par contraintes} et le solveur
\emph{ACE} (Abscon Constraint Engine). \texttt{pycsp.org}.

\bibitem{benzai} A.~Varet, N.~Prcovic, C.~Terrioux \emph{et~al.},
\emph{BenzAI: A Program to Design Benzenoids with Defined Properties
Using Constraint Programming}, Journal of Chemical Information and
Modeling, 2022. DOI: \texttt{10.1021/acs.jcim.2c00353}.

\bibitem{edmonds1965} J.~Edmonds,
\emph{Paths, trees, and flowers}, Canadian Journal of Mathematics, 17:449--467,
1965.

\bibitem{whitney} H.~Whitney,
\emph{Congruent graphs and the connectivity of graphs}, American Journal of
Mathematics, 54(1):150--168, 1932.

\bibitem{clar1972} E.~Clar,
\emph{The Aromatic Sextet}, John Wiley \& Sons, Londres, 1972.

\bibitem{huckel1931} E.~Hückel,
\emph{Quantentheoretische Beiträge zum Benzolproblem}, Zeitschrift für Physik,
70:204--286, 1931 (règle des $4n+2$ électrons $\pi$).

\bibitem{mackworth1977} A.~K.~Mackworth,
\emph{Consistency in Networks of Relations},
Artificial Intelligence, 8(1):99--118, 1977.

\bibitem{pauling} L.~Pauling,
\emph{The Nature of the Chemical Bond}, Cornell University Press, 3\textsuperscript{e}
édition, 1960.

\bibitem{randic} M.~Randić,
\emph{Aromaticity and conjugation}, Journal of the American Chemical Society,
99(2):444--450, 1977 (ordres de liaison et circuits conjugués).

\bibitem{rispoli2001} F.~J.~Rispoli,
\emph{Counting Perfect Matchings in Hexagonal Systems Associated with
Benzenoids}, Mathematics Magazine, 14:194--200, 2001.

\bibitem{kasteleyn1967} P.~W.~Kasteleyn,
\emph{Graph theory and crystal physics}, in \emph{Graph Theory and Theoretical
Physics}, Academic Press, p.~43--110, 1967.

\bibitem{stonewales} A.~J.~Stone, D.~J.~Wales,
\emph{Theoretical studies of icosahedral C$_{60}$ and some related species},
Chemical Physics Letters, 128(5--6):501--503, 1986.

\bibitem{xtb} C.~Bannwarth, S.~Ehlert, S.~Grimme,
\emph{GFN2-xTB — An Accurate and Broadly Parametrized Self-Consistent
Tight-Binding Quantum Chemical Method}, Journal of Chemical Theory and
Computation, 15(3):1652--1671, 2019.

\bibitem{cappart2023} Q.~Cappart, D.~Chételat, E.~Khalil, A.~Lodi,
C.~Morris, P.~Veličković,
\emph{Combinatorial Optimization and Reasoning with Graph Neural Networks},
Journal of Machine Learning Research, 24(130):1--61, 2023.

\bibitem{bengio2021} Y.~Bengio, A.~Lodi, A.~Prouvost,
\emph{Machine learning for combinatorial optimization: a methodological
tour d'horizon}, European Journal of Operational Research, 290(2):405--421,
2021.

\end{thebibliography}

\end{document}
